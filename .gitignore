# 实拍素材与音频：授权未核实，不入库（请自行放置到本地）
video/*
audio/*
# 程序化动画为原创，入库
!video/fx_market.mp4
!video/fx_flood.mp4
!video/poster/fx_market.jpg
!video/poster/fx_flood.jpg

# 运行/构建产物与临时文件（不入库）
_prof*/
_temp/
_check.js
# 构建与测试产物（不入库）
#   dist/ 装两样东西：素材分发包（体积大，走 GitHub Release 附件）+ 测试结果 JSON
#   （测试结果带时间戳，每次跑测试都会改写 —— 落在这里就不会污染 git status）
dist/
# 兜底：万一有脚本仍把结果写进 tests/，也不要进库（tests/ 只应放测试源码）
tests/*-results.json
tests/*_text.json
# 本地验证脚本（CDP 驱动，非游戏本体；归位后在 tools/verify/）
tools/verify/
# 出图流水线的 PNG 产物：由脚本重新生成，不入库（与 dist/test-results/ 同理）
测试截图/
*.bak
*.bak2
*.bak3
*.bf2bak
*.bf3bak
*.bf4bak
*.bf5bak
*.bf7bak
*.bf8bak
*.bf8bak0
*.mj2bak
*.mj3bak
*.mjbak
*.pickbak
*.fixbak
*.visbak
*.v14bak
*.v16bak
*.w35bak
*.bfbak
*.hintbak
*.layoutbak
*.log
_mj_render_out.txt

# 子代理调试中间产物（第三轮清理后追加，防止再次入库）
_bf_new_*.txt
_bf_jobs_*.json
_mj_*.txt
_probe_out/
_lovart_probe/
# mshta 探针的临时 .hta（正常情况下脚本跑完自己删；探针崩掉时会残留，兜底忽略）
_*.hta

# 编辑器/系统
.vscode/
.idea/
Thumbs.db
desktop.ini
