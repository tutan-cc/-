# 天枢（tutan123/tianshu）参考对比与借鉴落地报告

> 参考仓库：https://github.com/tutan123/tianshu （已下载到 `C:\Users\chris\Desktop\_tianshu_ref`，2410 文件 / 45MB）
> 对比对象：本项目 `重生2-原型`（单文件 HTML 真人互动影游）
> 结论：**两者技术路线不同、可以互补**——它是"3D 校园探索 + 数值玩法"，我们是"真人视频 + 章节叙事"。
> 已从中借鉴 7 项并全部落地验证（见第三节）。

---

## 一、两个项目的基本面

| 维度 | 天枢：重返江大 v3.2 | 重生2 原型 v0.6 |
|---|---|---|
| 形态 | 纯静态多文件项目（13 个 JS 模块 + 3 个 vendor 库） | 单文件 `index.html`（约 90KB）+ 素材目录 |
| 核心体验 | 3D 校园自由探索、室内 32 房间、8 印章任务 | 真人视频演出、选择/小游戏、剧情章节 |
| 演出 | 5 段 mp4 静音循环 + 4 张 jpg 打底，字幕推进 | 43 段实拍 mp4 逐镜 + 剧照兜底 + 离线 TTS 配音 |
| 玩法 | 6 类小游戏（问答/时机/线路/对决/回收/记忆） | 7 类小游戏（问答/时机/连打/躲避/股市/线路/记忆） |
| 成长 | 8 级 + 3 装备槽 + 6 装备 + 2 消耗品 + 商店 | 四维 + 9 羁绊 + **3 装备槽 + 6 装备（本次新增）** |
| 存档 | 版本化 + 四层校验 + 3 槽 + 导入导出 + 中途续玩 | **同上（本次借鉴落地）** |
| 测试 | 4 个 node 单测 + 5 个 Playwright 验收 + results.json | **1 个 node 单测（7 项）+ CDP 验收（7 项）+ results.json（本次新增）** |
| 素材治理 | `assets/SOURCES.md` 记录 CC0/MIT 与授权边界 | 未治理（本次补记，见第四节） |
| 3D | Three.js r149：InstancedMesh 合批、程序化纹理、BFS 寻路、第二台正交相机小地图、昼夜雨景 | 无（2D SVG 沙盘） |

**一句话**：它把"世界"做厚，我们把"演出"做厚。两者最大的公约数是 **状态机 + 存档 + 玩法参数化**，也正是本轮借鉴的主战场。

---

## 二、值得借鉴的点（按性价比排序）

| # | 借鉴点 | 出处 | 我们是否已落地 |
|---|---|---|---|
| 1 | `restore()` 唯一入口的四层校验（量纲→引用白名单→状态机边界→子系统补齐） | `state.js` | ✅ 已落地 |
| 2 | 演出状态机进存档（`active:{node,phase,shot}`）→ 中途退出原地续播 | `state.js` / `game.js` | ✅ 已落地 |
| 3 | `mediaToken` 令牌防竞态 + 图片打底视频叠加淡入 | `game.js:setMedia()` | ✅ 已落地 |
| 4 | 装备只产加成、接入玩法参数（时间/窗口/生命/本金） | `rpg.js` | ✅ 已落地 |
| 5 | 线路取证 circuit（整数旋转 + 全 0 校验） | `minigames.js` | ✅ 已落地 |
| 6 | 信号复原 memory（累计器驱动、错 3 次失败） | `minigames.js` | ✅ 已落地 |
| 7 | 验收契约：`assert(ok,name)` 即记录 → `*-results.json` + 失败置 exitCode | `tests/*.acceptance.js` + `run-browser.cjs` | ✅ 已落地 |
| 8 | `vm.createContext` 免 DOM 单测（把浏览器全局脚本当模块注入） | `tests/*.test.cjs` | ✅ 已落地 |
| 9 | 自动存档多点触发（节流 2s / 状态变更 / pagehide / visibilitychange） | `game.js` | ✅ 已落地 |
| 10 | flags + `needFlag`/`cost` 条件系统，**灰显而非隐藏** | `content.js` / `state.js` | ✅ 已落地 |
| 11 | 台词回顾 + 分镜进度条「02/05」 | `game.js` | ✅ 已落地 |
| 12 | 首奖只发一次（`done + results` 双记录） | `exploration.js` | 已有等价实现（`done[]`） |
| 13 | 3D 校园 / BFS 寻路 / 第二相机小地图 | `campus3d.js` | ⏸ 暂不采纳（见第五节） |
| 14 | Matter.js 物理回收小游戏 | `salvage.js` | ⏸ 可用 `Math.hypot` 免依赖复刻 |
| 15 | 谈判博弈（combat 改话术） | `minigames.js` | 🔜 建议下一轮做 |

---

## 三、本轮已落地的改造（全部有测试证据）

### 3.1 存档系统（借鉴 #1 #2 #9）

- `restore(raw)` 成为**唯一入口**：非 JSON / 字段缺失 / 未知节点 id / 负数值 → 一律返回 `null`，坏档进不了 UI
- 旧档自动迁移：缺 `rpg`/`flags` 字段时按默认值补齐（"缺省即重建"）
- `S.active={node,phase,shot}` 每镜写入 → **关掉浏览器再打开，从上次那一镜继续播**
- 存档点：状态变更时 + 电影层每 4 秒 + `pagehide` + `visibilitychange`
- 新增 **导出/导入 JSON**（导入必须过 `restore` 校验）

### 3.2 演出竞态与观感（借鉴 #3 #11）

- `mediaToken` 递增令牌：快速连点切镜时，旧视频/旧剧照回调一律作废
- 视频 `opacity` 过渡淡入（不再硬切），剧照 Ken Burns 永远垫底
- 顶部**分镜进度条** + 右上角**☰ 台词回顾**（最近 200 条，随存档保存）

### 3.3 装备系统（借鉴 #4）

6 件装备、3 个槽位（手部/身上/随身），**只产加成不改四维**：

| 装备 | 槽位 | 加成 | 接入的玩法参数 |
|---|---|---|---|
| 缠手带 | 手部 | 判定窗口 +6% | 拳击 QTE 绿区宽度 |
| 账本复刻 | 手部 | 答题时间 +4s | 答题倒计时 |
| 旧跑鞋 | 身上 | 额外生命 +1 | 数据洪流躲避 |
| 风衣 | 身上 | 判定窗口 +4% | 拳击 QTE |
| 内幕笔记 | 随身 | 本金 +600 | 股市波段初始资金 |
| 旧工牌 | 随身 | 答题时间 +6s | 答题倒计时 |

获得方式挂在剧情选择与挑战结果上（如"提醒林溪"→风衣、"拳击完美"→缠手带）。

### 3.4 两个新小游戏 + 两个新节点（借鉴 #5 #6）

- **线路取证**（Day4 上午 · 公司机房）：三段线路旋转归位，全 0 即取证成功 → 解锁成就【取证完毕】、获得「账本复刻」
- **信号复原**（Day7 下午 · 出租屋）：连记 3 组信号序列，错 3 次中断 → 解锁【前世之忆】
- 节点总数 14 → **16**，小游戏种类 5 → **7**

### 3.5 条件选项：灰显而非隐藏（借鉴 #10）

- `flags` 纯字符串数组（如「街面情报」「有人查你」「顾曼认可」）
- 选项支持 `needFlag` / `cost`：不满足时**灰显并说明原因**（"需要线索：街面情报"/"智慧不足 · 需要 15"），保留"本可以选"的张力
- 示例：酒吧给红姐的未来情报 → 解锁公园节点"换个方式谈"的隐藏选项

### 3.6 测试体系（借鉴 #7 #8）

```
node tests/core.test.cjs        # 7 项单测（vm 注入内核，毫秒级）
node _e2e.js                    # CDP 全流程验收 → tests/e2e-results.json
```

单测覆盖：素材存在性 / 剧情图连通无孤儿 / 效果键引用白名单 / 时间线单调 / 装备只改玩法参数 / 存档坏档拒绝与旧档迁移 / 续播快照校验。

**最近一次结果**：单测 **7/7 通过**；E2E **16/16 节点全通**（含线路取证、信号复原、躲避、股市、两种 QTE、两套答题、双章末卡），验收 **7/7 通过，0 失败**，产出物含装备 `[旧工牌/旧跑鞋/风衣/账本复刻]`、线索 `[街面情报/有人查你]`、成就 7 枚。

---

## 四、素材与许可治理（借鉴其 `assets/SOURCES.md`）

他们明确记录：Kenney 家具/自然包（CC0，Blender 转网格）、自建钟楼模型（附 `.blend` 与生成脚本）、Three.js/Matter.js/Lucide（MIT），并**显式声明旧版剧情影像授权未清理、不能视为版权结论**。

我们现状对照：

| 素材 | 来源 | 状态 |
|---|---|---|
| 43 段实拍视频 | `D:\重生2视频提取` | ⚠ 授权未核实，**发布前必须替换或取得授权** |
| 35 张剧照 | 上述视频抽帧 | 同上 |
| 2 段程序化动画 | 本项目 `_genanim2.ps1` 生成（.NET 绘图 + ffmpeg） | ✅ 原创，可自由使用 |
| 44 条配音 | 本项目 `_vo_gen.ps1`（Windows SAPI 离线合成） | ⚠ 依微软 TTS 使用条款，商用需确认 |
| 代码 | 本项目自研（架构仿天枢 v1.6 文档） | ✅ |

**建议**：项目根目录补一份 `SOURCES.md`，把上表固化下来——这是我们从他们仓库学到的最"便宜但最值钱"的一条纪律。

---

## 五、不采纳 / 后续可做

**不采纳（现阶段）**
- **3D 校园与 BFS 寻路**：与"真人视频互动影游"的体验重心不同；我们的沙盘只需承担"节点调度可视化"。若未来做"校园自由探索 + 触发视频"，其 `InstancedMesh` 合批、程序化纹理、第二相机小地图可直接复用。
- **Playwright 验收**：需要 npm 安装（本机无外网）；我们用 CDP + 原生 Chrome 实现了同等的"验收契约 + results.json"。
- **Matter.js 物理**：其回收小游戏只用点-圆查询，我们用 `Math.hypot` 即可零依赖复刻。

**建议下一轮**
1. **谈判博弈小游戏**（combat 改话术：施压/让利/试探/沉默，血条换"信任/筹码"）——与我们的羁绊系统天然契合
2. **零件回收**（免 Matter 复刻，重量-价值权衡）——可挂在"老城区/拆迁"节点
3. **多结局判定**：用他们 `state.ending()` 的优先级函数思路，按 flags + 羁绊阈值分档，替换我们目前的单一章末卡
4. **3 槽手动存档**：我们目前是单自动槽 + 导出/导入，加 3 个手动槽即可对齐
5. **谈判/结局相关的 flags 网**：目前只有 3 个线索，建议扩到 8-10 个形成结局分歧

---

## 六、如果要把两者"组合"成一个产品

三条可行路线：

| 路线 | 做法 | 工作量 |
|---|---|---|
| **A. 用天枢的"壳"装重生2的"肉"** | 保留其 3D 校园/室内探索，把"进入建筑 → 触发剧情"处替换为我们的真人视频节点 + 选择/小游戏 | 大（要移植 13 个模块的剧情接入点） |
| **B. 用重生2的"演出层"升级天枢** | 把其 5 段循环视频升级为"逐镜分镜 + 剧照兜底 + TTS 配音 + 台词回顾"，其余不动 | 中（我们的演出层已是独立可移植的一套） |
| **C. 只合并"工程底座"（推荐）** | 统一：`restore()` 存档校验、`active` 续播、`mediaToken`、装备加成层、验收契约 + results.json、SOURCES.md 素材治理 | 小（两边都已具备，抽成公共约定即可） |

**推荐 C**：两者题材相同（重生爽文）但玩法重心不同，硬合并会互相拖累；而工程底座是纯收益，且我们本轮已按 C 落地了大半。
