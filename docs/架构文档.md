# 重生2 · 重启人生 —— 原型技术架构文档

> 版本：v0.9（序章 + 第一章 + 第二章 · 17 节点 · 8 类小游戏 · 3D 沙盘 · RPG 成长 · 谈判博弈）
> 交付形态：**单文件 HTML + 本地视频/配音目录 + 一个 3D 模块**，零构建、双击即玩
> v0.8 新增（融合 `tutan123/tianshu` 的 3D 与 RPG）：
> Three.js 等距 3D 沙盘（`map3d.js` 1543 行：InstancedMesh 合批 + 主题色实例着色 + Raycaster 拾取 + 5 套氛围联动 + 小地图 + 2D 回落）·
> 8 级等级/称号被动 · 商店与 4 件消耗品（只 buff「下一场」）· 角色面板三标签
> v0.6 新增（借鉴工程底座）：`restore()` 四层存档校验 · 中途续播 · `mediaToken` 防竞态 ·
> 装备加成层 · 线路取证/信号复原 · 条件灰显 · 台词回顾 · 单测 + 验收契约 · `SOURCES.md`

> 版本：v0.6（序章 + 第一章 + 第二章 · 16 节点 47 镜 · 7 类小游戏 · 6 装备 · 44 条配音） · 架构仿照《天枢 · 重启大一》v1.6
> 交付形态：**单文件 HTML + 本地视频/配音目录**，零依赖、零构建、双击即玩
> v0.6 新增（借鉴 GitHub `tutan123/tianshu`，详见《天枢参考对比与借鉴落地报告》）：
> `restore()` 四层存档校验与旧档迁移 · 中途续播快照 · `mediaToken` 防竞态 · 装备加成层（只改玩法参数）·
> 线路取证/信号复原两个小游戏 · `flags` + `needFlag`/`cost` 条件灰显 · 分镜进度条与台词回顾 ·
> node 单测 + CDP 验收契约（产出 `tests/e2e-results.json`）· `SOURCES.md` 素材治理
> v0.5 新增：离线 TTS 配音层（44 条 · 13 音色 · BGM 自动闪避）
> v0.4 新增：三级画面兜底（视频→剧照KenBurns→动态渐变）· 启动自检与诊断面板 · 程序化动画片（.NET 逐帧 + ffmpeg 合成）
> v0.3 新增：躲闪 / 股市波段两个 Canvas 小游戏 · 章节卡转场 · 胶片质感层 · 字幕打字机
> v0.2 新增：台词画面逐镜对齐 · WebAudio 程序化音频层 · QTE 双模式 · 多题库 · 双章末卡

---

## 一、架构总览

### 1.1 设计哲学

| 原则 | 说明 |
|---|---|
| **数据驱动** | 全部剧情（镜头、台词、选项、效果、后继）声明在 `NODES` 一个 JSON 对象里，引擎代码零剧情硬编码 |
| **视频优先 + 兜底** | 镜头优先播放实拍 MP4；加载失败/缺失自动回落渐变背景 + 字幕，**任何素材缺失游戏都完整可玩** |
| **珍珠链叙事** | 主线固定主干（8 节点线性解锁），选择的重量靠 ①即时数值 toast ②羁绊累积 ③章末结算 体现，不做剧情网状分裂 |
| **不虐主红线** | QTE/答题失败走"惨胜"保底收益，无 BAD END |

### 1.2 三明治结构

```
┌─────────────────────────────────────────────┐
│  数据层  NODES / PLACES / BONDS / QUIZ / ACHV │  ← 策划只改这里
├─────────────────────────────────────────────┤
│  状态层  S（stats/bonds/done/achv/day/per）   │  ← localStorage 持久化
├─────────────────────────────────────────────┤
│  引擎层  渲染 / 演出 / 交互 / 调度             │  ← 程序只改这里
│  render() · playNode() · 选择/答题/QTE · 存档  │
└─────────────────────────────────────────────┘
```

### 1.3 文件结构

```
重生2-原型/
├── index.html          # 全部代码：CSS + HTML + JS（约 37KB）
├── video/              # 22 段实拍素材（720p H.264 + AAC，158MB，ffmpeg 转码自素材库）
├── 剧本-垂直切片.md      # 剧本与视频绑定表（策划文档）
├── 测试截图/            # E2E 自动化验证截图 ×6
├── _e2e.js             # CDP 全流程回归测试脚本（Node 24，无第三方依赖）
└── README.md
```

---

## 二、数据层（策划接口）

### 2.1 剧情节点 Schema

每个节点 = **shots（镜头序列）+ inter（交互）+ after（补镜）+ next（后继）**：

```js
nodeId: {
  loc:  "rent",            // 地图地点 id（PLACES 键）
  name: "出租屋 · 房东的底线",
  day:  1, per: 0,         // 第几天 · 时段（0上午/1下午/2晚上）
  ch:   1,                 // 章节
  shots: [{                // 正片镜头序列（依次播放）
    v:   "video/rent_door.mp4",        // 视频路径，可带 #t=秒 媒体片段定位
    t:   "台词/旁白文本",
    who: "芳姐",                        // 可选：说话人（金色字幕头）
    bg:  "radial-gradient(...)"         // 兜底背景（视频缺失时显示）
  }],
  inter: { ... },          // 交互（见 2.2），null = 纯剧情节点
  after: [{ ... }],        // 可选：交互完成后的补镜镜头
  next: ["park"],          // 后继节点 id（解锁链）
  end:  true               // 可选：章末节点，完成后弹结算卡
}
```

### 2.2 三种交互类型

| type | 场景 | 数据字段 |
|---|---|---|
| `choice` | 三选一决策 | `opts:[{t, fx(显示文案), r(效果), bonus:{need, ...}(条件追加)}]` |
| `quiz` | 答题小游戏 | `quiz:"lib"\|"biz"` 选择 `QUIZSETS` 题库；结果分 `win / part / lose` 三档 |
| `talk` | 谈判博弈 | 3 行动力/回合 · 施压/让利/试探 · 对手明牌 · 底线与筹码双条；胜负分 perfect/ok/miss |
| `qte` | 时机 / 连打 | `mode:"timing"`（绿区光标）或 `"mash"`（6 秒连打）；结果分 `perfect / ok / miss` |
| `dodge` | 躲闪小游戏 | 12 秒数据洪流，←→/鼠标移动；`perfect / ok / miss` |
| `stock` | 股市波段 | 六日买/卖/观望，Canvas K 线结算；`perfect / ok / miss` |

### 2.3 效果对象 `r` 的合法键（引擎 `applyFx()` 解析）

```js
{ cash:15, cha:5, phy:8, int:5,        // 四维数值（可为负）
  bond: { fang:10 },                   // 单人羁绊（BONDS 键）
  bondAll: 3 }                         // 全员羁绊
```

> 数值键、羁绊键、节点后继全部由 `_e2e.js` 同款校验逻辑自动检查（未知键/孤儿节点/断链/时间回退直接报错）。

### 2.4 全局数据表

| 常量 | 内容 |
|---|---|
| `PLACES` | 9 个地图地点（id / 名称 / 坐标 / 主题色） |
| `BONDS` | 9 名羁绊角色（id / 姓名 / 身份 / 头像 emoji） |
| `STATS` | 四维数值（cash 财富 / cha 魅力 / phy 体魄 / int 智慧） |
| `QUIZSETS` | 多题库答题（lib 讲题三连 / biz 风口三连问，含答案下标） |
| `ACHV` | 8 枚成就定义（含股神 / 身法） |

---

## 三、状态层

```js
S = {
  done:  ["intro","rent",...],   // 已完成节点（决定解锁）
  stats: {cash,cha,phy,int},     // 四维
  bonds: {fang,su,lin,wen,lei,hong},
  achv:  ["reborn",...],         // 已解锁成就
  day: 1, per: 0,                // 当前时间（HUD 时钟）
  koPerfect: false               // QTE 完美标记（成就用）
}
```

- **持久化**：每次 `render()` 自动 `localStorage.setItem("cs2_save", ...)`；标题屏点击开始时读取存档续玩
- **成就监听**：`checkAchv()` 在每次数值变动后扫描四个条件（通关序章 / 财富≥40 / 总羁绊≥60 / QTE完美）
- **称号**：`hudTitle()` 取最高维动态生成（"未来的资本家"等）

---

## 四、引擎层

### 4.1 界面分区（4 个固定层）

| 层 | id | 说明 |
|---|---|---|
| 标题屏 | `#title` | z-100，雨丝动画 + 主视觉，点击开始 |
| 主沙盘 | `#game` | HUD（头像/四维/时钟）+ SVG 地图 + 侧栏（羁绊/任务/成就） |
| 电影层 | `#cine` | z-60 全屏：`<video>` + 黑边 + 字幕 + 继续/跳过 + 各覆盖层 |
| Toast | `#toasts` | 右上角数值反馈浮条 |

### 4.2 演出状态机（核心）

```
playNode(id)
   │  phase="shots"
   ▼
playShot() ──视频播放──▶ 4.5s 后亮【继续 ▸】（视频播完/出错也会亮）
   │ 点击继续 / 空格
   ▼
advance() ──还有镜头──▶ playShot()（循环）
   │ 镜头播完
   ├─ phase==="after" ──▶ finishNode()
   ▼
inter 交互 ──choice▶ showChoice()  quiz▶ startQuiz()  qte▶ startQTE()
   │ 完成
   ▼
afterInter(r) ──applyFx() 结算──有 after──▶ phase="after", playShot()
   │ 无 after
   ▼
finishNode() ──标记 done──┬─ end 节点 ▶ showEndcard()（保持 #cine 打开）
                          └─ 普通节点 ▶ 关闭 #cine 回沙盘
```

> **踩坑记录**：章末卡 `#endcard` 挂在 `#cine` 内，`finishNode()` 若先关 `#cine` 再弹卡会导致结算卡永远不显示——E2E 截图发现，已修复（end 节点保持电影层打开，点"回到沙盘"才关闭）。

### 4.3 地图调度（时段解锁）

- `isAvail(id)`：节点的任一前驱已 done → 可玩（珍珠链单链实现）
- `renderMap()`：SVG 绘制网格 + 虚线路径 + 6 个 hotspot，三态 `avail`（脉冲光圈可点击）/ `done`（暗化）/ `locked`
- 进入节点时 `S.day/per` 推进到节点声明的时刻，时钟 HUD 同步

### 4.4 三个交互玩法实现

| 玩法 | 实现要点 |
|---|---|
| 选择 | 覆盖层面板；选项显示效果预览；`bonus.need` 支持"魅力≥10 时追加羁绊"这类条件分支 |
| 答题 | 12 秒/题 CSS 宽度动画倒计时；答对/答错即时着色 + 音效；700ms 后下一题；全胜/部分/全错三档结算；`QUIZSETS` 支持多题库（讲题/风口） |
| QTE·时机 | `requestAnimationFrame` 光标往返；绿区宽度 = `min(38, 14 + 体魄×0.8)%`（**数值直接改变判定手感**）；空格/点击判定，绿区中段 30%~70% 为完美 |
| QTE·连打 | 6 秒窗口，空格/点击计数，进度条即时反馈，按 NEED/OK 两阈值分三档结算 |
| 躲闪（Canvas） | `requestAnimationFrame` 主循环：方块按时间递增速度生成，玩家横条 ←→/鼠标移动；**判定带宽度 = 30 + min(28, 智慧×1.4)**，带内判为"闪开"不计伤害；生命 3 点，12 秒结算三档 |
| 股市波段（Canvas） | 预置六日收盘价（主角"记得"大势），每日买/卖/观望三键；实时绘制 K 线 + 均线 + 未来虚线提示；收盘按收益率分三档（≥80% / ≥20% / 保底） |

### 4.5 画面表现层　*v0.3 新增*

| 组件 | 作用 |
|---|---|
| 章节卡 `#dcard` | 进入新章节首个节点时全屏转场：CHAPTER 编号 + 章名 + `Day/时段/地点/节点名`，2.4 秒自动消散或点击跳过 |
| 调色/暗角 `#grade` `#vign` | 径向暗角 + 底部渐变，压出电影感边框 |
| 胶片颗粒 `#grain` | `repeating-conic-gradient` 3px 抖动动画，opacity .06 |
| 扫描线 `#scan` | 1px 水平重复纹理，opacity .16 |
| 字幕打字机 | 每 26ms 吐 2 字；进入新镜头自动打断上一段定时器，避免叠字 |
| 首帧就绪后再显示 | `onloadeddata` 里才 `classList.add("on")`，消除黑屏闪烁；`onerror` 回落渐变 + 一次性提示"建议用 Chrome 打开" |

### 4.6 音频层（WebAudio 程序化，零音频文件）　*v0.2 新增*

- **视频全部静音**（`<video muted>`）——原素材自带对白与字幕剧本不符，静音是"台词画面适配"的关键一步；
- `AudioSys.bgm(mood)`：5 种场景情绪（calm / city / tense / night / dark），每种是和弦表 + 波形 + 间隔的参数化循环，经低通滤波器进总线；节点声明 `mood` 字段，`playNode()` 自动切换；
- 音效：click / ding / good（上行琶音）/ bad（下行）全部振荡器合成；
- 音频上下文在标题屏点击时初始化（满足浏览器自动播放策略），电影层右上角 ♪ 可开关。

### 4.9 配音层（离线 TTS，零依赖）　*v0.5 新增*

原素材的演员台词与剧本不符，因此**视频一律静音**，改用离线 TTS 逐镜配音：

```
_vo_extract.js   ──▶  _vo_lines.json（从 NODES 抽出全部台词：40 条）
                            │
_vo_gen.ps1  ──▶  SAPI.SpVoice（Huihui 中文音色）──▶ PCM 22k/16bit ──▶ 手工补 WAV 头
                            │
                     ffmpeg 变调（asetrate+atempo，按角色 0.84×～1.11×）──▶ audio/vo/<节点>_<s|a><镜号>.mp3
```

- **文件名即索引**：`{nodeId}_{s|a}{shotIdx}.mp3`（`s`=正片镜头，`a`=交互后补镜），游戏无需在数据里标注路径
- **13 个角色差异化**：单一音色 + 变调变速率；旁白 0.93×、大师 0.84×、陈果 1.11× 等
- **BGM 闪避**：`duckBGM(true/false)` 把 `AudioSys.master.gain` 在 0.14 / 0.5 之间平滑切换
- **节奏控制**：`onloadedmetadata` 后按 `max(4.5s, 时长+0.5s)` 亮【继续 ▸】；点击画面任意处立即跳过本条语音
- **踩坑记录**：① WinRT 语音与 System.Speech 在本机均报错，只有 **COM `SAPI.SpVoice`** 可用；② `SpMemoryStream.GetData()` 返回的是**裸 PCM**，需手工补 44 字节 WAV 头；③ SAPI 在沙盒内被 `E_ACCESSDENIED` 拒绝，需完整权限才能合成。

### 4.12 装备加成层与条件系统　*v0.6 新增（借鉴 rpg.js / content.js）*

- **装备只产加成**：`ITEMS`（6 件）→ `SLOTS`（hand/outfit/accessory）→ `bonus()` 汇总 `{time,window,hp,capital}`，四维永不被装备改动
- **玩法参数接入点**（每处一行）：答题 `secs=12+bonus().time`；QTE `width=min(46,14+phy*0.8+bonus().window*100)`；躲避 `hp=3+bonus().hp`；股市 `cash=1200+bonus().capital`
- **条件选项**：`flags` 字符串数组 + `needFlag`（缺线索则灰显"需要线索：X"）/ `cost`（数值不足则灰显"智慧不足 · 需要 N"），**灰显而非隐藏**，保留"本可以选"的张力
- 装备面板（`#equip`）可换装，换装即时反映到下一场小游戏

### 4.13 测试契约　*v0.6 新增（借鉴 tests/ + run-browser.cjs）*

```
node tests/core.test.cjs   # vm 注入内核，7 项单测（素材/剧情图/效果键白名单/时间线/装备/存档/续播）
node _e2e.js               # CDP 全流程验收 → tests/e2e-results.json {success,testedAt,checks[],errors[],state}
```

- 单测用 `vm.createContext` 把 `index.html` 里的纯数据/纯逻辑段当模块注入（无需打包器与 DOM）
- 验收"断言即记录"：`assert(ok,name)` 把通过项推入 `checks[]`，失败置 `process.exitCode=1`，结果 JSON 可 diff 可归档
- 游戏内暴露显式调试钩子 `window.__cs2`（`S` / `node` / `phase` / `memSeq` / `cirAngles`），供验收脚本读取内部状态而不必去摸私有变量
- 注：本机沙盒会拦截 `node --test` 的子进程 spawn（EPERM），直接运行测试文件等价

### 4.10 视频播放管线（含三级兜底）　*v0.4 加固*

```
shot.v = "video/xxx.mp4#t=20"
   │
   ├─ ① <video> 加载 ──loadeddata──▶ 显示视频（首帧就绪才亮，消除黑屏闪烁）
   │        │
   │        └─ error / 2.6s 内 readyState<2 ──▶ 判定失效，mediaOK=false（本局锁定兜底）
   │
   ├─ ② 剧照兜底：video/poster/<同名>.jpg + Ken Burns 推移（transform scale 动画）
   │
   └─ ③ 动态渐变兜底：#shotbg 呼吸式亮度/饱和度动画
```

- **启动自检 `probeMedia()`**：分别探测一张剧照（Image）与一段视频（video metadata）；两者都取不到 → 弹诊断面板
- **诊断面板 `#mediawarn`**：展示当前 `location.href` + 诊断码（`no-media-files` / `no-video-frame` / `video-error`）与三条修复指引（解压 zip / 检查 video 目录 / 换 Chrome）
- **兜底仍可通关**：`mediaOK=false` 后跳过 video 分支，只走剧照/渐变，剧情、选择、小游戏全部照常

### 4.11 程序化动画生产（Remotion 的离线替代）　*v0.4 新增*

离线环境无法安装 Remotion（npm 不可达），改用**本机 .NET 绘图逐帧 + ffmpeg 合成**：

```
PowerShell + System.Drawing  ──▶  f0000.png … f0239.png  ──▶  ffmpeg -framerate 30 ──▶  fx_*.mp4
```

| 产物 | 内容 | 用途 |
|---|---|---|
| `fx_market.mp4` | 交易屏 K 线逐根生长 + 光标扫描 + 跳动报价 | 投资大厦 · 六个交易日 |
| `fx_flood.mp4` | 数据块下落 + 扫描线 + 标题故障抖动 | 老城区 · 躲闪小游戏前导 |

> 踩坑记录：PowerShell **变量名大小写不敏感**，`$n` 会覆盖 `$N`，导致 K 线只画出一根；改名 `$bars` 后修复。
> 另外 PS 5.1 读无 BOM 的 UTF-8 脚本会乱码，脚本需存为 **UTF-8 with BOM**。

**播放细节**：`ended` 与"4.5 秒保底"都会点亮【继续 ▸】（玩家不必看完整段）；媒体片段用 `#t=` 定位；素材生产统一 `ffmpeg scale=1280:-2, crf30, aac 96k, +faststart`（33 段实拍 2.4GB → 约 205MB）。

---

## 五、测试体系

| 层 | 工具 | 覆盖 |
|---|---|---|
| 语法 | `node --check` 提取 `<script>` | JS 语法错误（曾抓出中文引号嵌套 bug） |
| 数据校验 | Node eval 数据段 + 图遍历 | 视频引用存在性 / inter 类型 / 效果键与羁绊键合法性 / next 断链 / 孤儿节点 / 可达章末 / 题库答案下标 / 时间线单调 |
| E2E | `_e2e.js`（CDP 驱动 Chrome） | 页内注入"自动驾驶员"：点标题→打穿 14 节点（选择/答题/QTE/躲闪/股市全自动）→双章末卡；期间截屏关键画面 |
| 降级演练 | `_e2e_solo.js` | 把 index.html 单独放到无 `video/` 的目录，验证诊断面板 + 剧照/渐变兜底 + 剧情可继续 |

**最近一次 E2E 结果（v0.4）**：14/14 节点全通（含章节卡、双章末卡、时机 QTE、连打 QTE、躲闪、股市波段、两套答题、两段程序化动画）；
最终结算 财富100/魅力26/体魄14/智慧26/总羁绊109；历次截图比对先后抓出"章末卡被父容器隐藏""章节卡透出上一镜字幕""K线只画一根"三个真实 bug。

> 注意：E2E 前需删除 `_chromeprofile`（否则 localStorage 存档会让驱动找不到可玩节点）；无头模式截图里视频帧呈黑色是合成器限制，非播放故障（像素/readyState 已单独验证）。

---

## 六、扩展指南（加一个新节点只要 3 步）

1. **转码素材**：`ffmpeg -i 源.mp4 -vf scale=1280:-2 -c:v libx264 -crf 30 -preset fast -c:a aac -b:a 96k -movflags +faststart video/新名字.mp4`
2. **写数据**：在 `NODES` 里加一段（shots + inter + next），把它接进某个节点的 `next` 数组；若是新角色，在 `BONDS` 加一行
3. **跑校验**：`node _e2e.js`（或单独的数据校验）确认无断链、无未知键

新交互类型：在 `advance()` 的 type 分派处加一个分支 + 一个覆盖层 DOM + 一个 `startXXX()` 函数即可，演出/结算/存档全部自动继承。

## 七、已知边界

- 移动端（≤820px）隐藏侧栏，仅保留核心播放与交互；未做底部抽屉（天枢 v1.5 方案可迁移）
- 存档单槽位；无图鉴/回放；视频统一静音（台词以字幕呈现，TTS 配音是下一步候选）
- 第三章及以后（墨镜男的老板 / 顾曼的身份）：故事钩子已埋入 v0.2，素材库中 NPC / 待确认 / 过场动画分类仍有约 200 段未接线素材
