# 小游戏自动化调测指南

> 面向对象：接手的同事、以后来加小游戏的人。
> 目的：**把「改一行代码 → 开浏览器 → 手点十分钟 → 凭感觉觉得没坏」换成一条命令出结论。**
> 本文所有命令都在**仓库根目录**执行；所有路径都不写死本机目录，换机器直接能用。
>
> 本文写于项目定名「天枢原型」之后。`docs/` 下的历史报告写于改名之前，正文里仍作「重生2」——
> 那是对当时工作的真实记录，未作修改（见 `docs/README.md`）。

---

## 0. 一句话总览

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1
```

跑完会按层汇总通过数。想知道某一层为什么红，往下看对应章节。

---

## 1. 为什么不必每次开浏览器手调

小游戏的 bug 大致分三类，**成本差一个数量级**，所以要用三层金字塔分别兜：

| 层 | 跑什么 | 单次耗时 | 能测什么 | 测不了什么 | 本仓库对应脚本 |
|---|---|---|---|---|---|
| **第一层**<br>纯逻辑单测 | `vm` 注入内核 + 纯数据断言 | **0.1–40s** | 存档校验、剧情图连通、数值边界、装备加成、麻将 847 项规则、JSON 契约 | 画面对不对、DOM 有没有挂上、点击有没有反应 | `tests/*.test.cjs`、`tools/test/mahjong-logic.js`、`tools/dev/check-inline.js` |
| **第二层**<br>无头证据链 | mock DOM + 记录式 Canvas2D + 真去读 PNG 的 Image 替身 | **0.4–30s** | 「真跑生产代码」后：贴图有没有真画上去、画在哪个框里、回退链走没走、状态机对不对 | 字体排版、CSS 布局、真实输入事件、WebGL | `tools/bf/headless.js`、`tools/bf/shots/*`、`tools/bf/assets/shots*.js`、`tools/mj/shots*.js` |
| **第三层**<br>真浏览器 E2E | CDP 驱动真 Chrome；起不来就降级 mshta/Trident | **6–155s** | 真实渲染、真实鼠标事件、像素非空、四张验收图 | ——（最接近真人，但最慢最不稳） | `tools/e2e/mj-browser.js`、`tools/e2e/mj-system.js`、`tools/e2e/leisure.js`、`tools/e2e/main.js` |

**核心判断：一个改动只碰数值/规则，跑第一层就够；碰了绘制或素材，至少跑到第二层；碰了 DOM 结构、CSS、输入事件，才需要第三层。**

日常改代码，`-SkipBrowser` 那一档（第一层+第二层，约 45 秒）能挡掉绝大多数回归。

---

## 2. 第一层 · 纯逻辑单测

### 2.1 怎么跑

```powershell
node tests/core.test.cjs          # 内核：存档/剧情图/装备/时间线     → 8/8
node tests/breakfast.test.cjs     # 早餐店纯逻辑 + 贴图存在性        → 56/56
node tests/mj-system.test.cjs     # 麻将系统：赌注/邀约/口碑/情报     → 15/15
node tools/test/mahjong-logic.js  # 麻将规则（最大的一套）            → 847/847
node tools/dev/check-inline.js    # index.html 内联 <script> 语法闸  → 语法失败 0
```

> ⚠ **不要用 `node --test`**。本沙箱里 `node --test` 会用管道 spawn 子进程，直接 EPERM。
> 直接 `node xxx.test.cjs` 即可：这些脚本自己 `require("node:test")`，单独跑一样出 `ℹ pass N`。

### 2.2 `vm.createContext` 注入：不依赖浏览器跑生产代码

`index.html` 里那段 `<script>` 是**数据 + 逻辑 + UI 混在一条流里**的。测数据/逻辑时，
把需要的片段切出来丢进 `vm` 跑，就不用起浏览器：

```js
const ROOT = path.join(__dirname, "..");
const HTML = fs.readFileSync(path.join(ROOT, "index.html"), "utf8");
const SCRIPT = HTML.match(/<script>([\s\S]*?)<\/script>/)[1];

// 只取「数据段」：从 const PLACES 到音频层之前（纯数据，无 DOM 依赖）
function loadData() {
  const src = SCRIPT.slice(SCRIPT.indexOf("const PLACES"), SCRIPT.indexOf("/* ═══════════════ 音频层"));
  const ctx = vm.createContext({ console });
  vm.runInContext(src + "\n;globalThis.__D={PLACES,BONDS,STATS,NODES,QUIZSETS,ACHV};", ctx);
  return ctx.__D;
}
```

要点：

- **按锚点切片**（`indexOf` 起止），不要按行号——行号一改就烂。
- 只给被测代码**它真正需要**的宿主对象。比如测装备内核时给
  `{ localStorage: {...}, toast: () => {}, AudioSys: { good(){} } }`；给多了反而测不出「它偷偷依赖了 DOM」。
- **跨 realm 的坑**：`vm` 里造出来的对象/数组，原型链和宿主不同，`assert.deepStrictEqual` 会失败。
  一律先拼成字符串再比：
  ```js
  // ✗ 会挂：api.art.ready() 返回的是 vm 里的对象
  // assert.deepEqual(A.ready(), { food: 9, gear: 9, ... });
  // ✓ 拼字符串
  const readyStr = A => ["food","gear","face","ui","bg"].map(k => A.ready()[k]).join(",");
  assert.equal(readyStr(A1), "9,9,15,9,1");
  ```

### 2.3 断言怎么组织

两种形状，按文件选一种，别混：

**① `node:test` 形状**（`tests/*.test.cjs`，推荐新写测试用）：

```js
const test = require("node:test");
const assert = require("node:assert/strict");

test("存档校验：坏档一律拒绝，旧档自动迁移", () => {
  const C = loadCore();
  assert.equal(C.restore("not json"), null, "非 JSON 拒绝");
  assert.equal(C.restore(JSON.stringify({ stats: {}, bonds: {} })), null, "缺字段拒绝");
});
```

每个 `assert` 的第三个参数是**失败时唯一能看到的线索**，写成「期望什么」而不是「这里断言」：

```js
assert.equal(r2.bfLast.goal, 1, "目标数夹到下限 1（不合法即夹紧，非数字才回落默认 8）");
```

**② 自研 `A(cond, name, extra)` 形状**（`tools/bf/headless.js`、`tools/test/mahjong-logic.js`
这类要跑几百项的）：

```js
const A = (cond, name, extra) => cond ? (pass++, console.log("  ✔ " + name))
                                     : (fail++, console.log("  ✗ " + name + (extra ? "  ← " + extra : "")));
A(m.log.fills > 40 && m.log.strokes > 20, "首帧真的画了一堆矢量图元（底衬 / 边框 / 文字底）",
  m.log.fills + " fills / " + m.log.strokes + " strokes");
```

`extra` 一定要填**实测值**：失败时不用重跑就能看出「实际是多少」。

### 2.4 给新小游戏加用例的步骤

1. 在 `tests/` 下建 `你的玩法.test.cjs`，照 `breakfast.test.cjs` 的头几行抄（`ROOT` / 读源码 / `loadXxx()`）。
2. 先用一条「源码里真的存在这个契约」的断言把地基钉住——例如
   `assert.ok(/window\.Breakfast\s*=/.test(SRC), "对外契约 window.Breakfast 仍暴露")`。
   这样以后有人重构掉了出口，测试立刻红。
3. 再补数值/边界：阈值、上下限夹紧、非法输入、空数组、负数、`NaN`。
4. **素材存在性**要测，但只测「入库的那些」，并且跟项目约定一致（见 §5.6）。
5. 跑：`node tests/你的玩法.test.cjs`，确认 `ℹ pass N / ℹ fail 0`。

---

## 3. 第二层 · 无头证据链（不弹窗）

这一层是**本项目的看家本领**：在**完全不开浏览器**的前提下，把生产代码真跑一遍，
再把它的 Canvas2D 绘制指令**真光栅化成 PNG**。结论不是「我觉得应该画了」，而是
「`drawImage` 被调用了 43 次，其中 6 次的目标框落在 3 号盘位内，33 次画的是 `plate_bun.png`」。

### 3.1 怎么跑

```powershell
node tools/bf/headless.js          # 早餐店无头验收 → 通过 351，失败 0
node tools/bf/shots/panel.js       # 出图（会顺带调 powershell 合成中文）
node tools/bf/shots/panel2.js      # 第二批素材出图
node tools/mj/shots.js             # 麻将四张验收图
node tools/mj/shots-bg2.js         # 带骰子/筹码装饰的牌桌
```

### 3.2 原理：四个替身

| 替身 | 干什么 | 为什么必须真 |
|---|---|---|
| **mock DOM** | `getElementById` / `querySelector` / `addEventListener` / `getBoundingClientRect` 都实现成可控对象 | 生产代码要拿元素、量尺寸、挂监听；给 `null` 就崩，给假的就能观察它要了什么 |
| **记录式 Canvas2D** | 每个 `fillRect` / `arc` / `drawImage` 都**照做形状计算**并记下参数（`log.fills++`、`record.drawImage.push({src,dx,dy,dw,dh,argc})`） | 断言才有**实测值**可读；也能验证「有没有真的画」 |
| **真去文件系统读 PNG 的 `Image` 替身** | `img.src = v` 时同步读 PNG 头拿 `naturalWidth/Height`，成功就触发 `onload`，失败触发 `onerror` | 「贴图优先 + 矢量回退」这条链**只有真读盘**才测得出：文件在 → 贴图；不在/坏 → 必须走回退 |
| **可泵的 rAF / 虚拟时钟** | `requestAnimationFrame` 的回调存进数组，测试用 `pump(n)` 手动跑 n 帧；`Date`/`performance.now` 由可注入的 `clock` 驱动 | 时间可控 = 结果可复现；不会因为机器慢就少跑一帧 |

`Image` 替身的关键几行（照抄即可）：

```js
function ImageCtor() {
  return function Image() {
    return { naturalWidth: 0, naturalHeight: 0, complete: false, onload: null, onerror: null, _src: "",
      get src() { return this._src; },
      set src(v) {
        this._src = String(v);
        let sz = null;
        try { sz = pngSize(path.join(OUT, String(v).replace(/^\//, ""))); } catch (e) { sz = null; }
        if (sz) { this.naturalWidth = sz.w; this.naturalHeight = sz.h; this.complete = true;
                  if (this.onload) this.onload({ target: this }); }
        else if (this.onerror) this.onerror({ target: this });
      } };
  };
}
function pngSize(file) {                     // 只读头部，不解全图，够快
  const b = fs.readFileSync(file);
  if (b.length < 33 || b.readUInt32BE(0) !== 0x89504e47) throw new Error("不是 PNG：" + file);
  return { w: b.readUInt32BE(16), h: b.readUInt32BE(20) };
}
```

### 3.3 光栅化器 `tools/lib/raster.js`：把真绘制指令渲成 PNG

它零依赖（只用 `zlib`），自己解 PNG（zlib 解压 + 反过滤）、自己按变换做 `source-over` 合成、
自己编码 PNG。支持 `moveTo/lineTo/quadraticCurveTo/bezierCurveTo/arc/ellipse/rect`、
`fill/stroke/fillRect/strokeRect`、线性与径向渐变、`globalAlpha`、`lineWidth`、
`save/restore/translate/scale/rotate/setTransform`、非零环绕、4× 子像素抗锯齿。
中文画不了（只有 5×7 点阵 ASCII + CJK 块），所以说明文字交给 `tools/lib/text-compose.ps1`
用 System.Drawing + Microsoft YaHei 合成回去。

**它踩过的两个坑（改成新功能前必读）：**

1. **9 参 `drawImage` 的参数偏移**
   `drawImage(img, sx, sy, sw, sh, dx, dy, dw, dh)` 是 9 参重载，源矩形和目标矩形**不能按位置硬取**。
   早期实现只认 5 参形式，遇到 9 参就把 `sx` 当成了 `dx`——出图时贴图整体错位，
   而断言却是绿的（因为断言只看了「调用过」）。现在两处都按 `arguments.length` 分派，
   并把 `argc` 记进 `record.drawImage`，让断言能反过来检查「到底走的哪种重载」。
   ```js
   // 断言层：源/目标框都必须正尺寸，且 argc 只能是 5 或 9
   A(di.every(x => (x.argc === 5 || x.argc === 9) && x.dw > 0 && x.dh > 0 && x.sw > 0 && x.sh > 0),
     "drawImage 用 5 参（图+目标框）或 9 参（源矩形+目标框），源/目标框都是正尺寸",
     "例如 argc=" + di[0].argc + " " + di[0].sw + "×" + di[0].sh + " → " + di[0].dw + "×" + di[0].dh);
   ```
2. **按 `src` 命中缓存**
   同一张 PNG 一帧内会被画很多次（9 个锅位、6 个盘位…）。早期每次 `drawImage` 都重新解 PNG，
   出图慢到不可用，而且**每解一次就新建一份像素缓冲**，内存直接起飞。
   现在按 `img.src` 做解码缓存：解一次，之后只做变换 + 合成。
   ```js
   const cache = new Map();                       // src → {w,h,rgba}
   function decodeCached(src) {
     if (!cache.has(src)) cache.set(src, decodePNG(fs.readFileSync(toAbsPath(src))));
     return cache.get(src);
   }
   ```

### 3.4 怎么复用到新游戏

1. 生产代码必须暴露一个**可注入的挂载点**：`window.YourGame.start(hostEl, opts)`（见 §5）。
2. 新写一个 `tools/xx/headless.js`，照 `tools/bf/headless.js` 的头 100 行抄：
   `OUT = path.join(__dirname, "..", "..")` → 造 mock DOM → 造 rAF 泵 → `vm.runInContext` 跑生产代码 → `start()` → `pump(n)` → 断言。
3. **先测「契约」再测「画面」**：`start()` 返回 true、`isBusy()` 状态、`dispose()` 后 DOM 清空、
   `onFinish(result)` 被调用且 `result` 形状对。这些最容易在重构里被改坏。
4. 要出图时，加一个 `tools/xx/shots.js`：把 `createCanvas` 换成 `tools/lib/raster.js` 的实现，
   跑一帧后 `canvas.toPNG()` 落 `测试截图/`，再把 `texts` 写进 `dist/test-results/*_text.json`
   交给 `tools/lib/text-compose.ps1` 合成中文。

> **产物去向（重要）**：测试/出图产物一律落 `dist/test-results/`（不入库），
> 图片落 `测试截图/`（不入库）。**别写回 `tests/`**——`tests/` 只放测试源码。
> 也不要在脚本里写死这两个目录：用 `tools/lib/dist.js` 的 `resultsFile("x.json")`，
> 它会**先建目录再返回路径**（`dist/` 不入库，clone 下来没有，直接 `writeFileSync` 会 ENOENT）。

---

## 4. 第三层 · 真浏览器 E2E

### 4.1 怎么跑

```powershell
node tools/e2e/mj-browser.js     # 麻将浏览器实测 → 通过 142，失败 0
node tools/e2e/mj-system.js      # 麻将系统 E2E（mshta）→ 通过 115，失败 0
node tools/e2e/leisure.js        # 打斗 / 麻将 / 刮彩票
node tools/e2e/main.js           # 全流程回归（16 节点）
```

### 4.2 CDP 流程（模式 A）

脚本自己拉一个 headless Chrome，用 DevTools Protocol 驱动：

1. `spawn(chrome, ["--headless=new", "--remote-debugging-port=9228", "--window-size=1440,900",
   "--enable-unsafe-swiftshader", "--use-gl=angle", "--use-angle=swiftshader",
   `--user-data-dir=${OUT}\\_prof5`, "about:blank"])`
2. 轮询 `GET /json/version` 等它起来 → `PUT /json/new?<url>` 开标签页 → 连 WebSocket。
3. `Runtime.enable` / `Page.enable`，然后 `Runtime.evaluate` 点 DOM、`Page.navigate` 跳转、
   `Page.captureScreenshot` 出图。
4. 断言结果写 `dist/test-results/*-results.json`。

> `--enable-unsafe-swiftshader` + `--use-angle=swiftshader` 是**必须的**：headless 里没有 GPU，
> 不给软件渲染后端，`canvas` 会全黑，像素非空断言直接失败。

### 4.3 Chrome 起不来时自动降级到 `mshta.exe`

本机（以及很多受限环境）headless Chrome 起不来，典型报错：

```
mojo platform_channel 0x5  /  命名管道被拒
EXC:SecurityError ... is not valid JSON
```

所以脚本里都写了**两级降级**：

| 模式 | 引擎 | 怎么驱动的 |
|---|---|---|
| A | headless Chrome + CDP | `Runtime.evaluate` 注入 JS、`Page.captureScreenshot` 截图 |
| B | **`mshta.exe`（Trident / IE11 引擎）** | 生成一个 `.hta` 文件，里面 `src` 引同目录的 `mahjong.js`，跑完把结果写文本文件；Node 侧轮询读文件 |

**`mshta` 是什么**：Windows 自带的「HTML Application Host」。它把 `.html` 当**本地可执行程序**跑，
用系统里的 **Trident（IE11）引擎**渲染，而且允许 `ActiveXObject("Scripting.FileSystemObject")`
直接读写磁盘、允许 `window.resizeTo` / `moveTo`。本项目用它当「最后一个真引擎」——
它能把 `mahjong.js` 真渲染出来、能截 `canvas.toDataURL()`、能把 PNG 以 base64 写盘。

**为什么会弹出一个「M 开头的原生窗口」**：
`.hta` 是**真窗口程序**（不是浏览器标签页）。`mshta.exe` 跑起来就会创建一个原生窗口，
标题默认取 `<title>` 里的内容（本项目里叫 `domprobe` / `mjrender` 之类，任务栏/Alt-Tab 里显示为
「mshta」或那个标题）。窗口里跑的就是**这份 `.hta` + 它引用的 `mahjong.js`**，
没有任何菜单栏和地址栏——所以看起来像个"M 开头的怪窗口"。
它不是弹窗广告，是测试探针本体；**跑完脚本会把它 `kill` 掉**。

**怎么让它别干扰你**：

1. **首选：根本别跑第三层。** 日常改动用 §1 的第二层就够了：
   `powershell -File run-all-tests.ps1 -SkipBrowser`
2. **只跑某一层/某一个**：`-Only headless`，或直接 `node tools/e2e/mj-system.js`（它比 `mj-browser` 快得多、稳得多）。
3. **要跑就一次跑一个**：mshta 在同一会话里**连续跑几次之后会变得不可靠**
   （探针会「跑起来了、图也都写出来了，但最后那份 `_mj_render_out.txt` 结果文件没写成功」）。
   所以 `run-all-tests.ps1` 对浏览器层**默认自动重跑 2 次**（`-Retry N` 可调），
   每次重跑前会先 `Kill` 掉残留的 mshta。
4. **减小干扰**：探针自己会 `window.moveTo(0,0)` / `resizeTo(...)`，窗口会占屏幕。
   跑之前把别的窗口挪开、或者干脆接受它闪一下；跑完它会自己关闭。
5. **验证是不是「环境问题」而不是「代码问题」**：把命令**单独**在干净终端里跑一次。
   连续跑必失败、单独跑成功 = 环境问题（mshta 不稳），不是你的改动坏了。

### 4.4 截图出图链路

```
生产代码(mahjong.js/breakfast.js)
  → 模式A：CDP Page.captureScreenshot → base64 → 写 测试截图/*.png
  → 模式B：HTA 里 canvas.toDataURL("image/png") → base64 → 写 _mj_*_b64.txt
            → Node 侧读文本、base64 解码 → 写 测试截图/*.png
  → 需要中文说明条时：文字经 __textHook 记进 dist/test-results/*_text.json
            → powershell -File tools/lib/text-compose.ps1 -Manifest <json>
            → System.Drawing + Microsoft YaHei 把中文画回 PNG
```

---

## 5. 新小游戏接入骨架（契约）

新玩法要能被上面三层自动测到，**必须**满足下面的形状。这不是风格偏好，是测试能不能写出来的前提。

### 5.1 对外契约

```js
window.YourGame = {
  /** 挂载并开局。host = 容器元素；opts 里至少认 onFinish */
  start(host, opts) { /* ... */ return true; },
  /** 是否正在演出/等待输入（宿主据此决定要不要接管键盘） */
  isBusy() { return !!this._busy; },
  /** 收摊：清 DOM、清监听、停计时器。必须幂等 */
  dispose() { /* ... */ },
  /** 无头测试入口：把内部状态只读地吐出来 */
  debug: { state() {}, tick(dt) {}, /* 玩法特有：见 5.3 */ },
  /** 自证数据：出图/报告用，纯读不写 */
  test:  { /* ... */ }
};
```

硬性要求：

- **`start()` 同步返回结果**（`true` / `false`），不要只返回 Promise——无头测试要立刻知道成没成。
- **结算一律走 `opts.onFinish(result)`**，不要在内部直接改存档 / 弹自己的面板。
  `result` 是纯数据（`{ win, score, delta, ... }`），宿主负责落存档。
  这样第一层测「结算数据」、第二层测「演出」能彻底分开。
- **`dispose()` 幂等且彻底**：DOM 清空、`removeEventListener`、`clearTimeout/clearInterval` 全停。
  验收里专门有「剧情局收摊后 document 上不留 keydown 监听」这种断言。

### 5.2 确定性要求（**这一条最容易被忽略，也最致命**）

自动化测试要的是「同样的输入 → 同样的结果」。所以：

| 要求 | 做法 | 为什么 |
|---|---|---|
| **固定随机种子** | 牌堆/顾客池/奖面一律用可注入的种子；`debug` 里能读回种子 | 否则 847 项里有一项挂掉，你复现不出来 |
| **可注入时钟** | 不要直接 `Date.now()` / `performance.now()`；从注入的 `clock` 读 | 无头测试要能把时间快进 30 秒 |
| **`debug.tick(dt)` 有「补满」语义** | `tick(dt)` 必须把 `dt` 这段时间**完整推进**（含追赶逻辑），而不是只走一帧 | 测试里要 `tick(1/60)` 精确步进，也要 `tick(2.0)` 跳过动画 |
| **绘制只读状态** | `render()` 里不许改玩法状态 | 「先 step 再 render」的循环下，render 改状态会让帧语义错乱 |

> 本项目踩过的真实坑：`loop()` 是**先 `step()` 再 `render()`**，而 `step()` 会在订单完成时
> 立刻把顾客置 `left`（让出座位），`render()` 又只画 `!left` 的顾客 ——
> 结果用默认的 16ms 帧去泵，**「满意」那张脸永远抓不到**。
> 解法是用 **1ms** 那一帧（`game.pump(1, 1)`）：`1ms < 1/60s` 的步长阈值，这一帧只渲染、不推进。
> 教训写在这里：**出图/断言抓的「那一帧」是哪一帧，必须显式指定，不能靠默认值。**

### 5.3 贴图优先 + 矢量回退

素材可能缺（`video/` `audio/` `art/` 的部分内容不入库，靠素材包挂接）。所以每一个贴图点都要：

```js
if (drawAssetFit(g, assetOf(GEAR_ICON, "tools"), box, null)) IA.drawn.gearTools++;
else { /* 矢量兜底：原来的描边/填色画法 */ }
```

- 回退**至少两级**：`happy` 缺 → 用同角色的 `calm` → 再不行 → 矢量头像。
- 兜底分支要**计数**（`IA.drawn.xxx`），出图时打出来，能自证「这一帧走的是哪条路」。
- 统一加载器要能回答 `ready()`：`{ food: 9, gear: 9, face: 15, ui: 9, bg: 1 }`，
  并且**整批失败要整批判不可用**（同一个坏目录下，不能只成功一半）。

对应断言（第二层）：

```js
A(di.every(x => /(^|\/)art\/(bg\/kitchen\.png|icons\/([a-z]+\/)?[a-z_]+\.png)$/.test(x.src)),
  "每次 drawImage 的图都来自本地 art/（背景 / 食材 / 厨具 / 头像 / UI）", ...);
A(di.every(x => !/^https?:|^data:/i.test(x.src)), "drawImage 只画本地文件（没有 http(s) / data URI 外链）");
```

### 5.4 命名与目录约定

| 放哪 | 什么 |
|---|---|
| 仓库根 | **只放游戏本体 + 用户文档 + 素材脚本**（`index.html` `breakfast.js` `mahjong.js` `map3d.js` `three.min.js` `link-media.ps1` `pack-media.ps1` `媒体清单.json`） |
| `tools/lib/` | 共用库（`raster.js` 光栅化器、`text-compose.ps1` 中文合成、`dist.js` 产物目录） |
| `tools/test/` | 纯逻辑单测 |
| `tools/e2e/` | 端到端（CDP / mshta） |
| `tools/<玩法>/` | 该玩法的无头验收 + 出图 |
| `tools/dev/` | 改动工具（`patch-literal.js` 字面替换器、`check-inline.js` 语法闸） |
| `tests/` | **只放 `*.test.cjs`**，不放结果 |
| `dist/test-results/` | 测试与出图产物 JSON（**不入库**） |
| `测试截图/` | 出图 PNG（**不入库**） |
| `docs/` | 历史报告（写于改名前的正文保持原样） |

**`__dirname` 推算仓库根，层级按自身深度来**（写错就会去 `tools/` 里找 `index.html`）：

| 脚本位置 | 写法 |
|---|---|
| `tools/e2e/*.js`、`tools/bf/headless.js`、`tools/dev/*.js`、`tools/test/*.js` | `path.join(__dirname, "..", "..")` |
| `tools/bf/shots/*.js`、`tools/bf/assets/*.js`、`tools/bf/icons/*.js` | `path.join(__dirname, "..", "..", "..")` |
| `tools/lib/*.js` | `path.join(__dirname, "..", "..")` |

### 5.5 `.cjs` 还是 `.js`

仓库**没有 `package.json`**，Node 对 `.js` 默认就是 CommonJS。历史脚本用 `.cjs`，现已统一改成 `.js`。

> 以后若要加 `package.json`：**不要设 `"type": "module"`**，否则 `tools/` 下这些脚本会被当 ESM 解析而全部报错。

### 5.6 素材存在性断言怎么写才算对

素材分两类，**断言口径必须分开**：

| 类别 | 例子 | 能不能断言「磁盘上必须有」 |
|---|---|---|
| **入库的成品** | `art/icons/faces/stud_happy.png`、`art/bg/kitchen2.png` | ✅ 必须断言存在 + 尺寸 + 内容特征（四角透明等） |
| **可选的原始生成物 / 大素材** | `art/lovart_cca01cd7ba70.png`（切片源图）、`video/*.mp4`、`audio/*` | ⚠ 不能硬断言。要么不断言，要么**在库才校验** |

```js
/* 源图集按「素材契约」属于可选的原始生成物：在库就一并校验，不在库不算失败
   ——避免留下一条只有本机才过的断言（素材不入库是既有纪律，见 SOURCES.md 第五节）。 */
const grid = path.join(ROOT, "art", "lovart_cca01cd7ba70.png");
if (fs.existsSync(grid)) {
  const g = decodePngRGBA(grid);
  assert.ok(g.w === 2172 && g.h === 724, "源图集尺寸仍是 3 列 × 1 行（2172×724），切片参数未过时");
} else {
  console.log("  [skip] 源图集不在库（素材分发体系下属可选项）；切片产物已单独校验");
}
```

**判断标准**：一条断言如果在「同事 clone 完、还没跑 `link-media.ps1`」的机器上会红，
那它就不该是硬断言。素材缺失要走**回退链**，而不是让测试红。

---

## 6. 断言口径黄金法则

> **只断言「被操作动作自身的影响」，不要去读持续演化的世界状态。**

我们为此付过代价，具体三种翻车方式（都在 10/30 次随机运行里随机红）：

| 翻车写法 | 为什么必炸 | 正确写法 |
|---|---|---|
| 读 `served` 计数当进度 | `served` 会被 AI、超时、顾客离场从多个路径同时改；你只操作了一次，它可能加 0 也可能加 2 | 断言**动作本身的返回值/直接后果**：`drop("egg")` 之后这**一个**盘位的 state 变了 |
| 跨帧分数快照对比 | 帧与帧之间分数本来就在动（飘字、连续判定），`after > before` 不成立 | 断言**这一次操作**产生的增量：`onFinish` 收到的 `result.delta === 10` |
| 比较两位小数的浮点 | `0.1+0.2 !== 0.3`；渲染四舍五入后再比更随机 | 用整数（分/厘），或 `Math.round(x*100)`，或 `assert.ok(Math.abs(a-b) < 1e-6)` |

**怎么写出稳定断言——三个自检问题：**

1. **这个量在我操作之后，还有别人会改它吗？** 会 → 别读它，读我这动作的直接产物。
2. **把帧数改成 1 帧 / 100 帧，断言还成立吗？** 不成立 → 说明我在依赖某个特定帧，得显式指定帧（见 §5.2 的 1ms 帧）。
3. **把随机种子换一次，断言还成立吗？** 不成立 → 说明我把「恰好这次是某个值」写死了，要改成断言不变量（顺序、集合、单调性）。

**好的断言长什么样：**

```js
/* 「不再叠一份食材贴图」必须只看**盘位那一块**：底排食材桶每帧都要画 egg.png，
   整帧里数 egg.png 是数不准的。 */
const pbox3 = b5.B.plateBox(3);
const inPlate = name => b5.record.drawImage.some(r => r.src.split("/").pop() === name &&
  (r.dx + r.dw / 2) >= pbox3.x && (r.dx + r.dw / 2) <= pbox3.x + pbox3.w &&
  (r.dy + r.dh / 2) >= pbox3.y && (r.dy + r.dh / 2) <= pbox3.y + pbox3.h);
A(!inPlate("egg.png"), "盘贴图自带食物时，盘位框里不再叠一份食材贴图（避免一盘两样）",
  "盘位框 " + JSON.stringify(pbox3));
```

注意它同时做到了：**限定空间范围**（只看这一块）、**限定对象**（只看这一种贴图）、
**失败时给实测值**（打出盘位框）。

---

## 7. 常见坑清单

| 坑 | 症状 | 解法 |
|---|---|---|
| **`node --test` spawn EPERM** | `Error: spawn EPERM` / 管道被拒 | 直接 `node tests/xxx.test.cjs`；这些脚本自己 `require("node:test")` |
| **`python3` 是坏的 Store 存根** | 执行 `python3` 打开微软商店或报错 | 用 `python`（本机只装了 `python`） |
| **git fetch TLS 握手失败** | `schannel: next InitializeSecurityContext failed` | `git -c http.sslBackend=openssl fetch origin` |
| **PowerShell 变量名大小写不敏感** | `$Root` 和 `$root` 是同一个东西，覆盖了你没想覆盖的 | 变量名别只靠大小写区分（本项目踩过 `$src`/`$Src`） |
| **Node 改文件必须函数式 `replace(pat, () => rep)`** | 替换文本里的 `$&` `$1` 被当成反向引用，写坏了文件 | `s.replace(pat, () => rep)`；或直接用 `tools/dev/patch-literal.js` |
| **UTF-8 BOM** | PowerShell 5.1 把**无 BOM** 的 UTF-8 当 ANSI 读 → 中文乱码 + 解析报错（`The hash literal was incomplete`） | PS1 必须存成 **UTF-8 with BOM**；用编辑工具改完 PS1 **要复查 BOM 还在不在** |
| **`dist/` 不入库，clone 后不存在** | `ENOENT: no such file or directory ... dist\test-results\...` | 用 `tools/lib/dist.js` 的 `resultsFile()`（先建目录再返回路径） |
| **mshta 连续跑会失稳** | 探针「跑了、图也写了，但结果文件没写出来」→ exit 3 | 一次跑一个；`run-all-tests.ps1 -Retry 3`；跑前 `Get-Process mshta \| Kill` |
| **浏览器层 stdout 被重定向会坏** | `node tools/e2e/mj-browser.js > log.txt` 时探针失败，直接跑却成功 | 不要重定向浏览器脚本的 stdout；`run-all-tests.ps1` 已按层区分处理 |
| **改了大文件后没跑语法闸** | `index.html` 内联脚本坏了，游戏白屏 | `node tools/dev/check-inline.js`（改内联胶水层后必跑） |
| **`node --check` 不查 HTML** | `index.html` 语法坏了但 `node --check` 说 OK | 用 `check-inline.js`，它把每个内联 `<script>` 抽出来单独编译 |
| **在 `tests/` 里写结果** | 每跑一次测试 `git status` 就脏 | 结果写 `dist/test-results/`（已加 `.gitignore`） |
| **改大文件随手 replace** | 锚太短命中多处，改错地方 | 用 `node tools/dev/patch-literal.js <job.json>`：唯一性 + 幂等 + 备份 + 语法闸 + 失败回滚四道闸 |

---

## 8. 一条命令跑全部

```powershell
# 全跑（第一层 + 第二层 + 第三层）
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1

# 日常最快：跳过浏览器层（约 45 秒）
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1 -SkipBrowser

# 只跑某一层：logic / headless / browser
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1 -Only headless

# 失败自动重跑 3 次（浏览器层不稳时用）
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1 -Retry 3

# 打印每个脚本的完整输出（排查时用）
powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1 -ShowAll
```

输出形如：

```
 第一层 · 纯逻辑单测            通过   926   ✔ 全绿
 第二层 · 无头证据链            通过   351   ✔ 全绿
 第三层 · 真浏览器 E2E          通过   257   ✔ 全绿
 合计                     通过  1,534   用时 300.5s
```

脚本自己用 `$PSScriptRoot` 推算仓库根，**换机器 / 换用户名都不用改**；
每项的「期望通过数」只是基线参考，真正判失败的是**进程退出码**。

### 基线（2026-09-19 · rebase 到 `origin/main` 之后实测）

| 层 | 脚本 | 结果 |
|---|---|---|
| logic | `tests/core.test.cjs` | **8 / 8** |
| logic | `tests/breakfast.test.cjs` | **56 / 56** |
| logic | `tests/mj-system.test.cjs` | **15 / 15** |
| logic | `tools/test/mahjong-logic.js` | **847 / 847** |
| logic | `tools/dev/check-inline.js` | 内联脚本 1 段，**语法失败 0** |
| headless | `tools/bf/headless.js` | **351 / 351** |
| browser | `tools/e2e/mj-browser.js` | **142 / 142**（mshta 模式） |
| browser | `tools/e2e/mj-system.js` | **115 / 115**（mshta 模式） |

**已知问题（不是本轮改动引入的）**：`tools/e2e/leisure.js` 有 **2 项既有失败**
（「麻将面板打开且手牌 13 张」「麻将已结算」）——该脚本的 `dbgMj` 入口走的是麻将**系统**，
而断言取的是旧 `mahjong.js` 的字段，两边已不同步。**已在 `origin/main` 上复现同样 2 项失败**，
属既有问题，需要改断言（改成读 `mj-system` 的状态出口）而不是改玩法。
