# 早餐店 · 素材二批接入报告（Lovart 第二批 5 张）

> 本轮目标：把新生成的 5 张 Lovart 素材（盘面六宫格 / 早餐店背景 v2 / 顾客头像六宫格 /
> 麻将包间背景 / 麻将道具九宫格）**切片 → 接入 → 保回退**，并保证既有 847 项麻将逻辑单测与
> 126 项浏览器验收一项不退。
>
> 全部验收数字（真跑）：`node --check` 4 处 ✓ ｜ breakfast 单测 **55/55** ｜ core **8/8** ｜
> 无头证据链 **350/350** ｜ 麻将逻辑 **847/847** ｜ 麻将浏览器验收 **133/133**。
> 出图：`测试截图/bf_game_bg2.png` · `bf_plates6.png` · `mj_bg.png`（另有 `mj_icons9.png` 备查）。

---

## 一、源文件识别（按生成顺序，read_image 实际确认）

| 序号 | 源文件名 | 我实际看到的内容 | 用作 |
|---|---|---|---|
| 1 | `art/lovart_d227a60381a7.png`（1448×1086） | 3×2 六宫格、纯白底：**空盘 / 只装煎蛋 / 只装培根** ｜ **只装三明治 / 一笼包子（带蒸笼）/ 沙拉盘** | 盘面 6 张 → `art/icons/gear/plate_*.png` |
| 2 | `art/lovart_83728cd45e62.png`（2048×1152） | 早餐店店内景：**完整樱花树冠压满画面上缘与两侧**、正面敞开的门与街景、**右侧整墙开放厨房与货架**、前景大片干净木台面 | 背景 v2 → `art/bg/kitchen2.png` |
| 3 | `art/lovart_65d159fe6de7.png`（2016×1344） | 3×2 六宫格白底：**女房东（盘发·珍珠项链·驼色开衫）×（微笑平静 / 竖指着急 / 合掌大笑满意）** ｜ **女护士（护士帽红十字）×（平静 / 着急 / 满意）** | 头像 6 张 → `art/icons/faces/{fang,lu}_{calm,urgent,happy}.png` |
| 4 | `art/lovart_1a973d87de0e.png`（2048×1152） | **中式茶室麻将包间**：深绿绒布方桌（桌面留空）、四把木椅、暖光吊灯与宫灯、左侧博古架与圆窗、右侧夜景窗 | 麻将包间底 → `art/bg/mahjong.png` |
| 5 | `art/lovart_cef6ccfa89a5.png`（1024×1024） | 3×3 九宫格白底：**白板牌 / 發牌 / 两骰子** ｜ **蓝筹码 / 红筹码 / 金筹码堆** ｜ **深蓝斜纹牌背 / 木牌尺 / 烟灰缸** | 麻将道具 9 张 → `art/icons/mj/*.png` |

> 与任务表的「序号 4 = 麻将包间背景、序号 5 = 麻将道具九宫格」**顺序相反**：
> 第 4 个出现的文件（1024×1024）实际是**道具九宫格**，第 5 个（2048×1152）才是**包间背景**。
> 上级在 17:27 的补充说明也确认了这一点（第 4 张首次生成失败、重试后新出的才是包间背景）。
> 我按 **read_image 实际内容**归位，"按生成顺序"仅用于确定归属，没有硬凑。

识别过程留档：先用 `_bf_crop_probe.cjs --grid` 把每张源图按名义网格切小图、逐格 `read_image`，
再用 `_bf_box_probe.cjs` 打印每格内容包围盒（确认格线没有切到内容），最后才落切片脚本。

---

## 二、切片参数（全部由脚本自动求出，写在 `art/_assets2_report.json`）

工具：`_bf_assets2_gen.cjs`（零依赖，自带 PNG 解码/编码，逐行自适应过滤器）。
用法（源图**按生成顺序**传参，脚本自己查表，避免把随机 hex 文件名写死）：

```
node _bf_assets2_gen.cjs art/lovart_d227a60381a7.png art/lovart_83728cd45e62.png \
     art/lovart_65d159fe6de7.png art/lovart_1a973d87de0e.png art/lovart_cef6ccfa89a5.png
```

### 1）盘面六宫格 → `art/icons/gear/plate_*.png`（192×192 透明底）

- 网格 3×2，名义格 482.7×543.0；**逐行**找「整行/整列全白」的真实格线
  （第一行 y=550 处没有全白间隙 → 退化为「该带内内容最少的那条线」）。
- **白瓷盘压白底**：沿用上一轮的五步白底→透明，但参数换成盘面专用
  `NW_BG=252 / T1=244 / UP_MAX=16 / BAND=3`（默认 248/236 会把盘沿那圈浅灰当背景打穿）。
- 裁剪框 = 内容盒 × **1.10**（外扩 10%）并夹在本格内 → 盘沿完整、不吃邻居。
- 共用缩放 = (192 − 6×2) / 最大内容跨度 410 = **0.439024**（6 张盘同一比例，盘径视觉一致）。

| 文件 | 内容 | 裁剪框 | 余量(左/右/上/下) | 入画尺寸 | 不透明占比 |
|---|---|---|---|---|---|
| `plate_empty` | 空盘 | 460×416 @(33,84) | 39/27/30/31 | 178×159 | 60.5% |
| `plate_egg` | 只装煎蛋 | 450×422 @(493,84) | 28/25/30/31 | 178×162 | 61.6% |
| `plate_bacon` | 只装培根 | 464×422 @(943,84) | 18/47/30/31 | 179×162 | 62% |
| `plate_sandwich` | 只装三明治 | 465×431 @(22,533) | 45/22/30/32 | 180×166 | 63.3% |
| `plate_bun` | 只装包子 | 457×462 @(487,530) | 23/24/27/40 | 184×177 | 70.7% |
| `plate_salad` | 只装沙拉 | 465×433 @(944,532) | 20/47/31/31 | 179×166 | 63.6% |

### 2）顾客头像六宫格 → `art/icons/faces/{fang,lu}_*.png`（192×192 透明底）

- 网格 3×2（行 = 角色，列 = 平静/着急/满意）；真实格线**逐行**求
  （竖指 / 合掌那两格的手部会越过名义网格线；本图行1 列线 x=682/1349、行2 x=681/1349）。
- 白底→透明沿用上一轮参数（`NW_BG=248 / T1=236 / UP_MAX=20 / BAND=3`，人物边缘干净）。
- 共用缩放 = (192 − 8×2) / 最大内容跨度 631 = **0.278922**。

| 文件 | 内容 | 裁剪框 | 余量(左/右/上/下) | 入画尺寸 | 不透明占比 |
|---|---|---|---|---|---|
| `fang_calm` | 女房东·平静 | 468×636 @(162,21) | 13/14/18/19 | 126×169 | 41.5% |
| `fang_urgent` | 女房东·着急 | 539×636 @(717,21) | 15/16/20/17 | 144×168 | 46% |
| `fang_happy` | 女房东·满意 | 467×637 @(1397,20) | 13/14/18/19 | 126×169 | 42% |
| `lu_calm` | 女护士·平静 | 457×669 @(159,657) | 13/14/19/20 | 123×177 | 44% |
| `lu_urgent` | 女护士·着急 | 528×670 @(715,657) | 15/16/18/21 | 141×177 | 48.8% |
| `lu_happy` | 女护士·满意 | 461×670 @(1390,657) | 13/14/19/20 | 123×178 | 44.1% |

### 3）麻将道具九宫格 → `art/icons/mj/*.png`（256×256 透明底）

- 网格 3×3（1024×1024，名义格 341.3）；逐行真实列线：行1 x=341/658、行2 x=339/677、行3 x=327/656。
- 白底→透明同上（`NW_BG=252 / T1=244 / UP_MAX=16 / BAND=3`）；
  共用缩放 = (256 − 8×2) / 最大内容跨度 301 = **0.797342**。

| 文件 | 内容 | 裁剪框 | 余量(左/右/上/下) | 入画尺寸 | 不透明占比 |
|---|---|---|---|---|---|
| `tile_white` | 白板牌 | 223×310 @(79,33) | 6/7/9/10 | 174×238 | 59.4% |
| `tile_fa` | 發牌 | 219×310 @(389,36) | 6/7/9/10 | 171×238 | 58.5% |
| `dice` | 两骰子 | 307×212 @(665,131) | 9/10/6/7 | 235×165 | 45.3% |
| `chip_blue` | 蓝筹码 | 253×226 @(59,384) | 7/8/6/7 | 197×178 | 42.3% |
| `chip_red` | 红筹码 | 254×233 @(367,383) | 7/8/7/8 | 196×181 | 43.3% |
| `chip_gold` | 金筹码堆 | 254×248 @(705,380) | 7/8/7/8 | 196×192 | 49% |
| `tile_back` | 深蓝斜纹牌背 | 237×310 @(63,648) | 7/8/6/13 | 183×237 | 56.6% |
| `ruler` | 木牌尺 | 278×279 @(345,660) | 8/9/8/9 | 214×215 | 30.3% |
| `ashtray` | 烟灰缸 | 320×257 @(661,683) | 9/10/7/8 | 246×199 | 60.5% |

### 4）早餐店背景 v2 → `art/bg/kitchen2.png`（1180×790）

- 木台面上沿测量：中央带（x 512..1536）水平梯度能量 Top12，选中 **源 y=704**（能量 106.8，下方 +40px 平均色 rgb(246,182,94) = 暖色木 ✓）。
- **策略与代价（重点）**：源图 2048×1152（ar 1.778），画布 1180×790（ar 1.494）。
  - 若按 v1 公式把台面精确对到 `LAY.colHeaderY = 318`，需要 `scale = 472/(1152−704) = 1.0536`，
    源窗口只有 1120px 宽 → **左右各裁 464px（源宽 45%）**，樱花树冠与右侧货架正好被裁掉 ——
    而这正是 v2 要避免的。
  - 所以 v2 采用 **保整幅宽**：`scale = 1180/2048 = 0.5762`，**左右裁 0px**，源图顶边贴画面顶边。
  - 代价：台面在画布上落到 **y = 405.7**（不是 318）；画布底部比素材多出 **126px**，
    用「台面木纹条（源 y ≥ 704）纵向平铺 + 向底端线性压深 40%」补满（木纹横向连续，接缝不可见）。
  - 结果：**9 列元素位置一字未动**（盘带 328..438 / 锅带 450..608 / 桶带 624..776），
    台面上沿 405.7 落在盘带之内 → 盘/锅/桶三段**全部**画在木台面上；
    完整樱花树冠与完整右侧货架都在画面里（有像素级断言：左右 46px 边缘带仍有樱花色像素）。
- `art/bg/kitchen.png`（v1）**保留**，作为第二级回退。

### 5）麻将包间背景 → `art/bg/mahjong.png`（1721×1152）

- 16:9 cover 居中裁切：源 2048×1152 → crop 1721×1152 @(164,0)，**只裁左右 164/163px，上下不裁**。

### A. `breakfast.js`（逐字字面替换，4 批 patch，均已过语法闸）

1. **盘面按食物映射（修缺陷）**：`PLATE_TEX_OF_FOOD` 从 3 条（egg/bacon 共用 `plate_egg_bacon`）
   改成 5 条一一对应：`egg→plate_egg`、`bacon→plate_bacon`、`sandwich→plate_sandwich`、
   `bun→plate_bun`、`salad→plate_salad`；`plate_empty` 用于空盘与没有专属盘的食材（白粥/汤/果汁/牛奶）。
   `GEAR_ICON.ids` 9 → 12（新增 5 张盘、删掉旧的 `plate_egg_bacon`）。
   保留：火候进度环、糊了红叉、盘占用标记、双击半透明、9 列列对齐 —— 全部未动（有断言）。
2. **背景两级回退**：`BG_SLOTS = [kitchen2, kitchen]`（两张都预加载），
   `bgSource()/bgSlotActive()` 取第一张可用的；`bgDrawArgs()` 改成按**当前生效那张图自己的比例**算
   （换了素材不用改常量）；`counterTopCanvasY()` 按源窗口高换算（v2 → 405.7，v1 → 317.8）；
   `drawBg()` 里新增「底部木纹平铺补带」分支，程序化底原样保留为第三级回退。
3. **头像三态 + 5 人池**：
   - `FACE_KINDS` 3 → 5（新增 `fang` 女房东 / `lu` 女护士）；`FACE_ICON.ids` 6 → 12。
   - `faceMoodOf()`：`satisfied===true → happy`；否则耐心 > 0.40 → calm，≤ 0.40 → urgent。
   - `faceSatisfied(c)`：`order.length > 0 && done 覆盖全部 order`（与 `remainOf(c) <= 0` 同源，
     判据写成常量 `FACE_SATISFIED_RULE`）。
   - `facePoolOf(seed)`：用 `prand` + Fisher–Yates 打乱 5 位角色；种子常量 `FACE_POOL_SEED = 20240918`，
     **同一 seed 永远同一顺序**（顾客生成时按 `id % 池长` 取角色）。这样"5 位形象随机分配"且可复现。
   - 缺图回退：某角色没有 happy 贴图（本轮只有 fang/lu 有）→ 先退回该角色的 calm 脸，再不行才回退矢量头像。
   - **耐心条 / 订单卡 / 勾选 / 卡位布局一个都没动**（头像框仍是 64×68，卡位仍是 3 张）。
4. 新增测试出口：`art.background()/bgSlots()/plateTexIds/faceKinds/faceMoods/facePoolOf/faceMoodOf/`
   `faceSatisfied/faceSatisfiedRule/totalFiles()`，以及 `debug.serveAll(cid)`（把某位顾客订单一次补齐，
   供出图与断言验 happy 态；**只置 satisfiedAt、不置 left**，所以满意态停得住）。

### B. `mahjong.js`

1. 新增统一贴图加载器（模块初始化时预加载）：`art/icons/mj/*.png`（9 张）+ `art/bg/mahjong.png`，
   路径 = `页面目录 + art/…`（相对路径，无盘符/协议/data URI），`complete/naturalWidth` 判定就绪。
2. `drawFelt()`：最底层先用**包间背景贴图**等比 cover 铺满（水平居中），再叠 34% 暗场；
   贴图不可用 → **原样回退程序化绿绒**（径向渐变那段一行没删）。
3. `drawTileBack()`：牌背优先用 `tile_back` 贴图（等比塞满 + 圆角 clip），
   不可用 → **原样回退程序化深蓝斜纹**（原代码整体搬进 else 分支，未做任何修改）。
4. 新增 `debug.art()`：报告 9 张贴图 / 背景的就绪状态、`roomBg`、`tileBackTex` 使用次数（无头与浏览器验收都读它）。
5. **未改动**：赣麻规则、AI、提示算法、结算面板、牌墙/座位/牌河/手牌排序/金框脉动 —— 一律零改动
   （847 项逻辑单测 + 133 项浏览器验收全过即为证据）。

### C. 测试

- `tests/breakfast.test.cjs`：51 → **55** 项。改写 5 条旧断言（贴图张数/背景路径/几何/就绪数/头像池），
  新增 4 项：① 盘面 6 张在磁盘上真实存在，且「按食物映射」的 5 张用**不透明像素平均色差 > 6** 证明互不相同
  （老版本 egg/bacon 差值为 0，这条会直接挂）；② 背景 v2 优先 + 两级回退 + 保整幅宽 + 底部木纹补带；
  ③ 头像三态 + 5 人池（固定种子可复现、满意判据常量、布局不变式 64×68/3 卡位）；
  ④ fang/lu 三态 6 张在磁盘上齐全且互不相同。
- `_e2e_bf_headless.cjs`：337 → **350** 项。改写背景段断言（改成与 `art/_assets2_report.json` 逐字一致 +
  保整幅宽 + 底部补带），新增「煎蛋盘 ≠ 培根盘」「三态 happy」「5 人池可复现」。
- `_test_mahjong_logic.cjs`：**847/847**（未改测试，仅确认没退）。
- `_e2e_mahjong2.js`：126 → **133** 项。HTA 探针新增 6 项（9 张贴图就绪 / 背景就绪 / 相对路径 /
  本帧真的铺了包间背景 / 牌背用了贴图 / art 出口可读），Node 侧新增 1 项
  **像素级取证**：直接解 `mahjong_table.png`，量「画面左侧与顶部是暖木色（R > G + 6）」且
  「牌桌中心不是旧程序化绿绒那种饱和绿（G − R < 15）」→ 证明照片背景真的铺上去了，不是源代码里有字符串。

### D. 工具/脚本

- 新增 `_bf_assets2_gen.cjs`（切片 + 两级背景裁切 + 报告）、`_bf_assets2_shots.cjs`（盘面/道具平铺图）、
  `_bf_shots2.cjs`（`bf_game_bg2.png` / `mj_bg.png` 真跑出图）。
- `_bf_raster.cjs` 两处增强（都是为了让新素材能出图，不改既有行为）：
  ① `createCanvas(w,h,{hostWidth,hostHeight,hostDpr})` —— 支持按位图尺寸开缓冲
  （mahjong.js 会 `cv.width = W*dpr`，否则整屏画到画面外）；② 补 `clearRect`（mahjong 每帧第一件事就是它）。

---

## 四、全部验收数字（真跑）

| 检查 | 命令 | 结果 |
|---|---|---|
| 语法 | `node --check breakfast.js` / `mahjong.js` / `_bf_raster.cjs` | 三处 exit 0 |
| 语法 | `index.html` 内联脚本 1 块（vm.Script 编译） | 0 失败 |
| 早餐单测 | `node tests/breakfast.test.cjs` | **55 / 55** |
| 核心单测 | `node tests/core.test.cjs` | **8 / 8** |
| 早餐无头链 | `node _e2e_bf_headless.cjs` | **350 / 350** |
| 麻将逻辑 | `node _test_mahjong_logic.cjs` | **847 / 847** |
| 麻将浏览器验收 | `node _e2e_mahjong2.js`（模式 trident-hta） | **133 / 133** |
| 切片报告 | `art/_assets2_report.json` | 21 个图标文件 + 2 张背景，0 疑似空白 |

出图（`测试截图/`）：

| 图 | 尺寸 | 内容 |
|---|---|---|
| `bf_game_bg2.png` | 2360×1684 | 真跑 breakfast.js：背景 v2（完整树冠+货架）· 盘带同时出现**空盘/煎蛋/培根/三明治/包子/沙拉 6 种盘**（还有一列糊了红叉）· 三张顾客卡 `stud_happy / office_calm / uncle_urgent`（三态同框）· 底部说明条 |
| `bf_plates6.png` | 816×740 | 6 种盘面平铺（棋盘底 + 中文标注映射） |
| `mj_bg.png` | 2480×1720 | 真跑 mahjong.js：包间背景铺满 + 绿绒方桌 + 四把木椅 + **四家牌墙/手牌背面全部用深蓝斜纹牌背贴图** + 手牌/弃牌/判牌面板 |
| `mj_icons9.png` | 724×912 | 麻将道具 9 张平铺（含 tile_back 标注「★已接入」） |

---

## 五、未接上 / 不满意 / 需要拍板的地方（诚实清单）

1. **★ 背景 v2 的木台面上沿没能落在 y=318，实测 405.7（差 87.7px）**。
   这是**数学上的硬冲突**，不是没做：源图 ar 1.778、画布 ar 1.494。要让台面落在 318，
   可视宽只能是 1120px（左右各裁 464px = 源宽 45%）——樱花树冠与右侧货架就没了一大半，
   与 v2 的目的（"不再裁掉树冠与右侧货架"）直接冲突。
   我按"**两侧内容一个都不许裁**"这条更硬的约束走了**保整幅宽**，代价是台面下移到盘带之内（405.7），
   `LAY.colHeaderY = 318` 与 9 列元素坐标**代码里一字未改**，盘/锅/桶三段依然全部落在木台面上。
   底部多出的 126px 用台面木纹纵向平铺补齐（不是纯色渐变，接缝不可见）。
   **如果你更看重"台面必须精确 318"，把 `_bf_assets2_gen.cjs` 里的 `BF_K2_ALIGNED=1` 环境变量打开重跑即可**
   （脚本已支持这条分支：scale 1.0536 / 左右各裁 464px / 底部不补带），代价就是树冠与货架各裁掉约 23%。
2. **happy（满意）态只有女房东与女护士两张贴图**，学生/白领/大爷这 3 位角色的 happy 没切
   （本批头像六宫格只给了 2 位角色 × 3 态）。运行期这 3 位的满意态会**先退回该角色的平静脸**
   （比回退矢量头像自然），`_e2e` 里有断言明确记录"这 3 张已知缺图"。要补齐需要再生成一张"3 位老角色 × 满意"的头像图。
3. **麻将道具只接入了牌背，骰子/筹码/牌尺/烟灰缸/白板/發 入库未接入**。
   它们都是等距静物图（不是俯视牌面视角），直接贴到牌桌上会与现有扁平牌面风格冲突；
   牌背是唯一"必须换"的一处（原来只有程序化斜纹），已经换掉且保留程序化回退。
   骰子留了一个明确的可接点：牌桌中央 `LAYOUT.center`，但目前中央画的是余牌/巡数文字，接图会挡住它，所以没动。
4. **`art/icons/gear/plate_egg_bacon.png` 已被弃用但文件仍在磁盘上**（没有引用它的代码了）。
   我保留它作为"旧盘子"的历史素材，没有删除；如果要清理，删掉它不影响任何测试
   （`GEAR_ICON.ids` 里已经没有这个 id，联调时不会有 404）。
5. **`art/bg/kitchen.png`（v1）也保留着**，占 881KB。它是第二级回退，建议留着。
6. 包间背景铺满画布后，牌桌坐标（`LAYOUT.felt` 等）**仍是原程序化布局**，
   因此照片里的绿绒方桌与程序化绿绒面板**不完全重合**——现在是"照片房间 + 程序化牌桌"叠着看，
   视觉上成立（暗场压过之后像"桌上铺了块深绿绒"），但严格说不是"贴图桌面对齐牌桌"。
   真要完全对齐需要按贴图重算 `LAYOUT.felt`，那样**所有牌位/牌河/命中区都要动**，
   会威胁 847 + 133 项断言，我选择不动（任务也明确要求"必须保留牌墙/座位/牌河/手牌排序/金框脉动"）。
7. 无头出图链在本沙箱仍是"软件光栅化 + 系统字体合成"，不是真浏览器截图
   （Chrome/Edge 起不来是老问题）；麻将浏览器验收走 mshta/Trident 真引擎（133 项全过）。
8. 顺手做的小修：`_bf_raster.cjs` 加了 `clearRect` 与"按位图尺寸开缓冲"的可选参数
   （不改默认行为，`bf_game_bg.png` 等上一轮的图仍可复现）。

---

## 六、复现步骤

```bat
cd 重生2-原型
node _bf_assets2_gen.cjs art/lovart_d227a60381a7.png art/lovart_83728cd45e62.png art/lovart_65d159fe6de7.png art/lovart_1a973d87de0e.png art/lovart_cef6ccfa89a5.png
node _bf_assets2_shots.cjs          :: 盘面 / 麻将道具平铺图
node _bf_shots2.cjs                 :: bf_game_bg2.png / mj_bg.png
powershell -NoProfile -ExecutionPolicy Bypass -File _bf_text.ps1 -Manifest tests\bf_shots2_text.json
node tests/breakfast.test.cjs && node tests/core.test.cjs
node _e2e_bf_headless.cjs
node _test_mahjong_logic.cjs && node _e2e_mahjong2.js
```

> 背景如果改成"台面精确 318"那一版：`set BF_K2_ALIGNED=1 && node _bf_assets2_gen.cjs …`（Windows）。

### 仓库里留下的脚本（可复现，其他中间文件已清掉）

| 脚本 | 作用 |
|---|---|
| `_bf_assets2_gen.cjs` | 本批 5 张源图 → 21 张图标 + 2 张背景（切片 + 两级背景裁切 + 写 `art/_assets2_report.json`） |
| `_bf_assets2_shots.cjs` | `测试截图/bf_plates6.png` · `mj_icons9.png`（贴图平铺 + 中文标注） |
| `_bf_shots2.cjs` | `测试截图/bf_game_bg2.png` · `mj_bg.png`（真跑 breakfast.js / mahjong.js + 软件光栅化） |
| `_bf_patch.cjs` | 逐字字面替换工具（唯一性 / 幂等 / 备份 / 语法闸 / 失败整文件回滚）——沿用上一轮 |
| `_bf_text.ps1` | 出图后的中文合成（System.Drawing + Microsoft YaHei）——沿用上一轮 |

改动过的文件都留了备份：`breakfast.js.bf8bak0` / `mahjong.js.bf8bak0` / `index.html.bf8bak0`（本轮开工前）、
`*.bf8bak`（patch 工具自动写的"改之前"版本）。`index.html` 本轮**没有改动**（只做了内联脚本语法校验）。
