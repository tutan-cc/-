# 调试与跳转说明

## 一、标题屏调试按钮（最省事）

打开 `index.html` 后，标题屏「开始重生」下方有两个按钮：

| 按钮 | 作用 |
|---|---|
| **⚑ 跳到谈判（调试）** | 直接把前置 15 个节点标记为已完成，并**立刻进入「合同谈判」玩法**（Day9 上午 · 金色年华）。赠送：财富 120、四维 22/18/40、红姐与顾曼羁绊 24（**因此谈判开局就吃到羁绊加成：底线 −8、筹码 +3**）、5 件装备、4 种消耗品各若干 |
| **⚑ 全部解锁** | 保留 ktv 为可玩，其余节点全部标记完成，回到沙盘随便逛 |

> 调试跳转只写 `localStorage`，不影响正式流程；想恢复干净进度用下面的「重置」。

## 二、控制台接口（更细的调试）

按 F12 打开控制台，可用 `CS2_DEBUG`：

```js
CS2_DEBUG.nodes()              // 列出全部节点 id
CS2_DEBUG.jump('ktv')          // 跳到任意节点（前置自动补齐？否——用 unlockAll 先解锁）
CS2_DEBUG.unlockAll()          // 解锁全部节点（保留 ktv 可玩）
CS2_DEBUG.give(500)            // 财富 +500
CS2_DEBUG.state()              // 打印当前存档对象
CS2_DEBUG.reset()              // 清空存档并刷新
// ── 麻将系统 ──
CS2_DEBUG.mjRecord()           // {wins, losses, net, rep, streak}
CS2_DEBUG.mjSet({mjNet:999})   // 直接改麻将相关字段（写盘 + 重绘）
CS2_DEBUG.mjLobby()            // 打开「找人打两圈」开局面板
CS2_DEBUG.mjFree(200,'gentle') // 直接开一局：注码 ¥200 · 放水
CS2_DEBUG.mjInvite('hong')     // 立刻触发 NPC 邀约（id: hong / man / guo）
```

> 邀约判定只在**回到沙盘**时发生（节点结算 / 章末卡「回到沙盘」）。想在控制台立刻看邀约条：
> `CS2_DEBUG.mjSet({bonds:{...S.bonds, hong:20}, per:2, stats:{...S.stats, cash:2000}})` 然后 `CS2_DEBUG.mjInvite('hong')`。

## 二·五、麻将系统（赌注 / 自由局 / 邀约 / 口碑 / 情报）

侧栏「🀄 找人打两圈」是常驻入口（与「随身装备」「刮刮乐」并列），点开是**开局面板**：

| 面板项 | 说明 |
|---|---|
| ① 选注码 | **¥50 / ¥200 / ¥1000** 三档；每档写明「小胡 每家 = 2 张 × 注码」；**财富低于该档的置灰并写明原因**（如「财富不足（需要 ¥200，当前 ¥60）」） |
| ② 选打法 | **认真打**（智脑提示全开 · 赢面大）/ **放水陪玩**（智脑提示本局关闭且不可开 · 出牌走温和版次优解 · 涨好感） |
| ③ 开局 | 点「开局」直接开桌；「再想想」关闭面板。面板底部常显战绩（胜/负 · 净收支 · 牌友口碑 · 当前财富） |

- **赔付**：每家应付 = 张数 × 注码（小胡 2 张 / 大胡 8 张 / 大大胡 12 张 / 最大胡 24 张），胡家共收 = 3 家之和；抢杠仍是「杠家包赔三家」；流局不结算。结算亮牌板新增一行：注码、每家应付、共收、你的净收支。
- **保底**：财富不足付不起时按 ¥0 兜底，**不会出现负财富/破产**。
- **邀约**：回到沙盘时若 `bonds.hong ≥ 20 且 per=2`（晚上）→ 红姐约 ¥200 的局；`bonds.man ≥ 20` → 顾曼约 ¥1000；`bonds.guo ≥ 20` → 妹妹约 ¥50（纯涨羁绊）。同一条邀约**接受过就不再出现**；点「改天」后 **20 分钟内**不再骚扰。
- **羁绊联动**：只有邀约局结算才动该角色羁绊——认真且「赢太多」（净胜 > 2×注码）扣 2~4；放水 +3（让对方胡 +6）；妹妹局恒定 +4。
- **牌友口碑**（`S.mjRep`，0~100，初始 50）：胡牌 +3、三连胡再 +4、杠被抢包赔 −5、抢杠胡 +2、杠上开花 +1、放水 −2、认真赢太多 +3、单局大亏 −1。
- **牌桌情报**（赢下这一桌解锁，放水到对家胡只解锁对方那条）：

| 解锁方式 | 得到 flag | 用在哪 |
|---|---|---|
| 赢金老板 | `金老板的软肋` | 「合同谈判」多一条隐藏杀招「摊 牌（牌桌）」：底线 −18 · 筹码 +2 · 谈成额外 财富 +30 · 红姐 +4 |
| 赢红姐 | `红姐的铺面情报` | 「1978 酒吧」多一条隐藏选项（财富 +25 · 智慧 +6 · 红姐 +14） |
| 赢顾曼 | `顾曼的底线` | 预留（后续章节用） |
| 赢赵家（后续可扩） | `赵家的资金链` | 预留 |

- **成就**：`mjWin` 牌桌老千（原有）/ `mjStreak3` 三连胡 / `mjPayer` 包赔三家 / `mjRival` 冤家（单局净亏 ≥ 2×注码）。
- **剧情节点不变**：金色年华麻将局仍然默认「认真」、只给节点奖励（perfect/ok/miss），不额外扣财富；`Mahjong.start(host, {onFinish})` 的旧调用行为与旧档完全兼容（不传 stake 即 ¥10 基准）。

### 验收脚本（本地）

```bash
node _test_mahjong_logic.cjs     # 麻将纯逻辑单测（含赌注/放水/邀约/口碑/情报）
node tests/core.test.cjs         # 核心内核 7/7
node tests/mj-system.test.cjs    # index.html 麻将胶水层 + 旧档兼容 + 跨模块对接
node _e2e_mahjong2.js            # 牌桌浏览器实测（mshta/Trident 降级）
node _e2e_mj_system.js           # 赌注/自由局/邀约浏览器实测 + 3 张出图
```

出图：`测试截图/mj_stake_panel.png`（开局面板）· `测试截图/mj_result_pay.png`（结算赔付明细）· `测试截图/mj_invite.png`（邀约条）。

## 三、直接改 localStorage 存档

存档键名：**`cs2_save`**（JSON 字符串）。控制台里：

```js
// ① 查看当前存档
JSON.parse(localStorage.getItem('cs2_save'))

// ② 一键跳到谈判（等价于「跳到谈判」按钮）
localStorage.setItem('cs2_save', JSON.stringify({
  version: 2,
  done: ["intro","rent","park","office","library","boxing","bar","audit","chase","sister","flashback","nurse","stock","investor","master"],
  stats: {cash:120, cha:22, phy:18, int:40},
  bonds: {fang:14, su:16, lin:18, wen:12, lei:12, hong:24, guo:14, lu:12, man:24},
  flags: ["街面情报","有人查你","顾曼认可"],
  achv: ["reborn","cash40","charm60"],
  day: 9, per: 0, card1: 1, card2: 1,
  rpg: { xp:280, owned:["badge","notes","wrist","ledger","sneakers"],
         equipped:{hand:"wrist", outfit:"sneakers", accessory:"notes"}, bag:{coffee:2} },
  buffs: {time:0, window:0, hp:0, capital:0},
  voMode: "real", history: [], active: null
}));
location.reload();

// ③ 只改数值（例如把羁绊拉满，看谈判加成上限）
const s = JSON.parse(localStorage.getItem('cs2_save'));
s.bonds.hong = 40; s.bonds.man = 40; s.stats.cash = 999;
localStorage.setItem('cs2_save', JSON.stringify(s)); location.reload();

// ④ 重置
localStorage.removeItem('cs2_save'); location.reload();
```

### 存档字段说明

| 字段 | 说明 |
|---|---|
| `done` | 已完成节点 id 数组；**未知 id 会被存档校验拒绝**（防止坏档进 UI） |
| `stats` | 四维：cash 财富 / cha 魅力 / phy 体魄 / int 智慧 |
| `bonds` | 九人羁绊：fang 芳姐 / su 苏晚晴 / lin 林溪 / wen 温阮 / lei 雷姐 / hong 红姐 / guo 陈果 / lu 白露 / man 顾曼 |
| `flags` | 线索字符串数组（如「街面情报」可解锁公园节点的隐藏选项） |
| `rpg.equipped` | 装备槽：hand / outfit / accessory |
| `rpg.bag` | 消耗品：coffee / wrap / coin / energy |
| `buffs` | 「下一场」临时加成：time 秒数 / window 判定窗口 / hp 生命 / capital 本金 |
| `voMode` | 配音模式：`real` 真人原声 / `tts` 合成语音 / `off` 关闭 |
| `active` | 演出续播快照 `{node, phase, shot}`；非法值会被丢弃 |
| `mjWins` / `mjLosses` / `mjNet` | 麻将战绩：胜场 / 负场 / 累计净收支（自由局 + 邀约局都计；剧情局的麻将局只计胜负、不计净收支，因为它的财富来自节点奖励） |
| `mjRep` | 牌友口碑 0~100（初始 50） |
| `mjStreak` | 当前连胡数（输一局清零） |
| `mjInvites` | 邀约记录 `{hong:{t,mode}}`：`mode:"accepted"` 本轮不再弹、`"declined"` 20 分钟内不再骚扰 |

> 旧档（没有 `mj*` 字段）**不会被判成坏档**：`restore()` 会补默认值（`mjRep=50`、其余 0、`mjInvites={}`），
> 越界值会被夹紧（如 `mjRep:999 → 100`、`mjWins:-3 → 0`），未知邀约 id 直接丢弃。见 `tests/mj-system.test.cjs`。

> ⚠ 存档写入前会过 `restore()` 四层校验：量纲 → 引用白名单（`done`/`achv` 必须存在于内容表）→ 状态机边界 → 子系统缺省补齐。所以手改时**节点 id、成就 id、装备 id 必须拼写正确**，否则整份存档会被判为坏档（当前进度保留）。
